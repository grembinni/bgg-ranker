---
phase: 04-display-polish
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/components/ComparisonView.test.tsx
  - src/store/store.test.ts
autonomous: true
requirements:
  - DISP-01
  - DISP-02

must_haves:
  truths:
    - "Failing tests for thumbnail/placeholder behaviour exist before GameCard is changed"
    - "Failing tests for upset callout render exist before store or ComparisonView is changed"
    - "Failing tests for hamburger menu exist before ComparisonView is changed"
    - "Failing tests for pick() upset detection exist before store is changed"
    - "Failing tests for login() auto-resume exist before store is changed"
  artifacts:
    - path: "src/components/ComparisonView.test.tsx"
      provides: "Phase 4 describe blocks for DISP-01, DISP-02, D-08 hamburger"
      contains: "describe.*thumbnail|describe.*callout|describe.*hamburger"
    - path: "src/store/store.test.ts"
      provides: "Phase 4 describe blocks for D-03 pick upset, D-07 login auto-resume"
      contains: "describe.*upset|describe.*auto-resume"
  key_links:
    - from: "src/components/ComparisonView.test.tsx"
      to: "src/components/ComparisonView.tsx"
      via: "vi.mock('../store/store', ...) must include lastUpset and logout in mock object"
      pattern: "lastUpset.*null"
    - from: "src/store/store.test.ts"
      to: "src/store/store.ts"
      via: "pick() called with lower-ranked winner → lastUpset set"
      pattern: "lastUpset"
---

<objective>
Wave 0: Extend the two existing test files with failing describe blocks for all Phase 4 behaviours.

Purpose: Establish the RED phase of TDD before any production code is touched. Tests must fail immediately after being added (since the store and components do not yet have lastUpset, logout, hamburger, or thumbnail/placeholder changes). Subsequent plans (04-02, 04-03, 04-04) make them green.

Output: Both test files extended with new describe blocks. Running `npm test -- src/components/ComparisonView.test.tsx src/store/store.test.ts` produces failures (RED state confirmed).
</objective>

<execution_context>
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-VALIDATION.md
</execution_context>

<context>
@D:\dev\repo\bgg-ranker\.planning\PROJECT.md
@D:\dev\repo\bgg-ranker\.planning\ROADMAP.md
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ComparisonView.test.tsx (current mock object — lines 33-56):
The vi.mock block's selector object currently has these fields:
  currentPair, sessionComparisons, comparisonsTotal, sessionUsername, sessionId,
  dirtyGameIds, unplayedIds, startSync, pick, skip, refresh, markUnplayed,
  showRankedList, showUnplayedList, games, ratings.
Phase 4 requires adding: lastUpset (default null), logout (vi.fn()).
Note: startSync moves to hamburger — it must remain in mock because hamburger calls it.
The mock game objects use thumbnail: '' (empty string — triggers placeholder path after upgrade).
To test thumbnail presence, add a game with thumbnail: 'https://example.com/thumb.jpg'.

From src/store/store.ts (pick action — lines 335-353):
  pick(winnerId, loserId) reads ratings, calls applyUpset, sets new ratings.
  Phase 4 extends pick() to also set lastUpset when winnerPos > loserPos.

From src/store/store.ts (login action — lines 423-438):
  login(username, password) calls bggLogin, then calls fetchCollection(username).
  Phase 4 changes login() to check rankingsUsername/ratings/games BEFORE calling fetchCollection.

From src/store/store.ts (ComparisonStateSlice — lines 63-73):
  lastUpset field does NOT yet exist. Tests referencing it will fail (RED).

Store helpers in store.test.ts:
  createMockStorage(), makeGames(n), makeRatings(n), setupStoreWithGames(games, ratings, rankingsUsername)
  All helpers are already defined and can be reused in new describe blocks.
  bggLogin mock: vi.mocked(mockBggLogin).mockResolvedValueOnce({ sessionId: 'sess123' })
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend ComparisonView.test.tsx with Phase 4 failing describe blocks</name>
  <files>src/components/ComparisonView.test.tsx</files>
  <read_first>
    - src/components/ComparisonView.test.tsx (full file — understand current mock structure, imports, describe style before adding)
    - .planning/phases/04-display-polish/04-CONTEXT.md (D-01 through D-09 decisions)
    - .planning/phases/04-display-polish/04-PATTERNS.md (Pattern 2 callout, Pattern 3 hamburger, Pattern 4 thumbnail)
  </read_first>
  <behavior>
    STEP 1 — Extend the vi.mock selector object (before all describe blocks):
      Add lastUpset: null and logout: vi.fn() to the mock object.
      Add a second mock variable: let mockLastUpset: { winnerName: string; spotsGained: number } | null = null
      Reset it in beforeEach: mockLastUpset = null
      Update the mock to reference mockLastUpset for lastUpset field.

    STEP 2 — Add game with thumbnail to mock games object:
      games.g0 currently has thumbnail: '' — keep as is (placeholder case).
      Add games.g2 with thumbnail: 'https://example.com/thumb.jpg' to test image render.
      Since currentPair is ['g0', 'g1'], the thumbnail test needs currentPair to include g2,
      OR test the placeholder path with g0/g1 (both have thumbnail: '').
      Strategy: add a second currentPair variable mockCurrentPair that can be overridden,
      then add g2 to games and in thumbnail tests set mockCurrentPair = ['g2', 'g1'].

    STEP 3 — New describe block: 'GameCard thumbnail (DISP-01)'
      Test 1: when thumbnail URL exists, renders an img element (query: screen.getByRole('img', { name: /game c/i }) or getAllByRole('img'))
      Test 2: when thumbnail is empty string, renders placeholder div containing text "No image"
      Test 3: when thumbnail URL exists, img is wrapped in a link to boardgamegeek.com/boardgame/{id}

    STEP 4 — New describe block: 'Upset callout (DISP-02)'
      Test 1: renders nothing when lastUpset is null (no element with text "moved up")
      Test 2: renders callout text "Game A moved up 3 spots" when lastUpset = { winnerName: 'Game A', spotsGained: 3 }
      Test 3: uses singular "spot" when spotsGained === 1: "Game A moved up 1 spot"
      Test 4: callout has amber styling class — query container and check className includes 'bg-amber-50'

    STEP 5 — New describe block: 'Hamburger menu (D-08, D-09)'
      Test 1: hamburger button (aria-label "Menu") is rendered in the header
      Test 2: dropdown is NOT visible initially (no "Sync to BGG", "Refresh rankings", "Logout" text in document initially — or they have hidden attribute)
      Test 3: clicking hamburger button reveals "Sync to BGG", "Refresh rankings", "Logout" items
      Test 4: "Sync to BGG" item is disabled when syncDisabled (dirtyGameIds empty)
      Test 5: clicking "Logout" calls logout() from store
      Test 6: clicking "Refresh rankings" calls refresh() from store
      Test 7: standalone Refresh button is NOT in the action bar (no button with text /^Refresh$/ outside menu context — NOTE: this test is subtle; use getAllByRole and filter, or just confirm the menu contains Refresh)
  </behavior>
  <action>
    Add to the top of the mock variable declarations section (after existing mockShowUnplayedList):
      const mockLogout = vi.fn()
      let mockLastUpset: null | { winnerName: string; spotsGained: number } = null
      let mockCurrentPair: [string, string] = ['g0', 'g1']

    Update vi.mock selector object to include:
      currentPair: mockCurrentPair  (change from hardcoded ['g0', 'g1'])
      lastUpset: mockLastUpset
      logout: mockLogout
      games: { g0: { id: 'g0', name: 'Game A', yearPublished: 2020, thumbnail: '' },
               g1: { id: 'g1', name: 'Game B', yearPublished: 2021, thumbnail: '' },
               g2: { id: 'g2', name: 'Game C', yearPublished: 2022, thumbnail: 'https://example.com/thumb.jpg' } }

    Update beforeEach to also reset:
      mockLogout.mockReset() (or include in vi.clearAllMocks() which already runs)
      mockLastUpset = null
      mockCurrentPair = ['g0', 'g1']

    Append three new describe blocks after the existing 'ComparisonView Sync to BGG button' describe:

    describe('GameCard thumbnail (DISP-01)', ...) — 3 tests as specified in behavior
    describe('Upset callout (DISP-02)', ...) — 4 tests as specified in behavior
    describe('Hamburger menu (D-08, D-09)', ...) — 7 tests as specified in behavior

    CRITICAL: Do NOT modify existing tests. Only add new mock fields and new describe blocks.
    Do NOT use fenced code blocks in this action — the executor reads the file and the patterns file for exact syntax.
    Existing Sync to BGG tests will need updating since Sync moves to hamburger dropdown — but hold off: those tests check a header button that will be removed. Mark them as expected-to-change with a TODO comment: // TODO(04-04): these tests will be updated when Sync moves to hamburger
    Actually: do NOT modify existing describe block. The executor will see the conflict during implementation in 04-04.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/components/ComparisonView.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    File has three new describe blocks (thumbnail, callout, hamburger).
    Mock object includes lastUpset and logout fields.
    npm test on ComparisonView.test.tsx shows failures in the new describe blocks (RED state).
    Existing passing tests remain passing (mock extension is backward-compatible).
  </done>
</task>

<task type="tdd">
  <name>Task 2: Extend store.test.ts with pick() upset detection and login() auto-resume failing tests</name>
  <files>src/store/store.test.ts</files>
  <read_first>
    - src/store/store.test.ts (full file — understand existing describe blocks, helpers, mock patterns)
    - .planning/phases/04-display-polish/04-CONTEXT.md (D-01 through D-03 for pick; D-07 for login)
    - .planning/phases/04-display-polish/04-PATTERNS.md (Pattern 1 timer, Pattern 5 login auto-resume)
    - src/store/store.ts (lines 335-438: pick() and login() implementations to understand what to assert)
  </read_first>
  <behavior>
    STEP 1 — New describe block: 'pick() upset detection (D-01, D-02, D-03)'
      Setup: setupStoreWithGames with makeGames(4) and explicit ratings so g3=900, g2=700, g1=500, g0=300
             (g0 is ranked lowest, g3 highest)
      Test 1 (upset fires): pick('g0', 'g3') — g0 was ranked 4th (pos 3), g3 was ranked 1st (pos 0)
        → lastUpset should be non-null
        → lastUpset.winnerName should be 'Game 0' (g0's name from makeGames)
        → lastUpset.spotsGained should be 3 (loserPos 0 - winnerPos 3 → 3, but: winnerPos > loserPos means winner was lower, spotsGained = winnerPos - loserPos = 3)
      Test 2 (no callout when winner was higher-ranked): pick('g3', 'g0') — g3 was ranked 1st, g0 was ranked 4th → normal result, NOT an upset → lastUpset should be null
      Test 3 (timer fires clear): use vi.useFakeTimers(); pick upset; verify lastUpset non-null; advance 5000ms; verify lastUpset is null. Use vi.useRealTimers() in afterEach.

    STEP 2 — New describe block: 'login() auto-resume (D-07)'
      Setup: createAppStore with stored state: rankingsUsername='alice', ratings (non-empty), games (non-empty)
      Mock: vi.mocked(mockBggLogin).mockResolvedValueOnce({ sessionId: 'sess123' })
      Test 1 (auto-resume — same user with rankings): await store.getState().login('alice', 'pw')
        → mockBggFetch should NOT have been called (no collection fetch)
        → store.getState().view should be 'comparison' (continueSession navigates there)
        → store.getState().sessionId should be 'sess123'
      Test 2 (fetch — different user): setup with rankingsUsername='bob', then login('alice', 'pw')
        → mockBggFetch SHOULD have been called
      Test 3 (fetch — same user, no ratings): setup with rankingsUsername='alice', ratings={}, then login('alice', 'pw')
        → mockBggFetch SHOULD have been called
  </behavior>
  <action>
    After the last existing describe block in store.test.ts, append two new describe blocks:

    describe('pick() upset detection (D-01, D-02, D-03)', () => { ... }) — 3 tests as in behavior.
    describe('login() auto-resume (D-07)', () => { ... }) — 3 tests as in behavior.

    For the timer test, use vi.useFakeTimers() / vi.useRealTimers() pattern.
    For the auto-resume test: store.setState({ rankingsUsername: 'alice', ratings: makeRatings(3), games: makeGames(3) })
      then call await store.getState().login('alice', 'pw')
      The continueSession() action sets view to 'comparison' — assert on that.

    Note on RED state: lastUpset does not exist on the store yet (ComparisonStateSlice lacks the field).
    The tests will fail with TypeScript compile errors or runtime undefined access. That is acceptable —
    Wave 0 establishes RED; 04-02 makes these green.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/store/store.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    File has two new describe blocks (pick upset detection, login auto-resume).
    Existing passing tests remain passing.
    New tests fail with missing field or incorrect behaviour (RED state confirmed).
    Commit: "test(04-01): add failing Wave 0 tests for DISP-01, DISP-02, D-03, D-07, D-08"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → mock store | Test mock supplies controlled values; no real network or storage calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm installs | accept | No new packages in Phase 4; audit table in RESEARCH.md confirms zero installs |
</threat_model>

<verification>
After both tasks complete:
  npm test -- src/components/ComparisonView.test.tsx src/store/store.test.ts

Expected: new describe blocks RED (failing), existing tests still GREEN.
The mix of red and green is the correct Wave 0 exit state.
</verification>

<success_criteria>
- src/components/ComparisonView.test.tsx contains describe blocks: 'GameCard thumbnail (DISP-01)', 'Upset callout (DISP-02)', 'Hamburger menu (D-08, D-09)'
- src/store/store.test.ts contains describe blocks: 'pick() upset detection (D-01, D-02, D-03)', 'login() auto-resume (D-07)'
- Mock object in ComparisonView.test.tsx includes lastUpset and logout fields
- All previously-passing tests remain green
- New tests fail with appropriate errors (RED state)
</success_criteria>

<output>
Create .planning/phases/04-display-polish/04-01-SUMMARY.md when done
</output>