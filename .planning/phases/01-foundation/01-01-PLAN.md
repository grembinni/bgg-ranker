---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - vitest.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/engine/.gitkeep
  - src/api/bggClient.ts
  - src/store/store.ts
  - src/components/.gitkeep
  - .env.development
  - .env.production
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run dev starts Vite without errors and the browser shows 'BGG Ranker'"
    - "npm run test (npx vitest run) is runnable and exits without configuration errors"
    - "All Phase 1 dependencies are installed and resolvable (react 19, zustand 5, vitest 4, etc.)"
    - "Full folder structure exists so Phase 2 can begin coding immediately with no reorganization"
    - "VITE_BGG_API_BASE env var is defined for both dev and prod environments"
  artifacts:
    - path: "package.json"
      provides: "Full tech stack dependencies"
      contains: "react, zustand, @tanstack/react-query, vitest, tailwindcss"
    - path: "vite.config.ts"
      provides: "Vite build config with React + Tailwind plugins"
      exports: ["default"]
    - path: "vitest.config.ts"
      provides: "Vitest config: node environment, globals: true"
      contains: "environment: 'node'"
    - path: "src/App.tsx"
      provides: "Minimal app shell rendering 'BGG Ranker'"
      contains: "BGG Ranker"
    - path: "src/api/bggClient.ts"
      provides: "Placeholder file for Phase 2 implementation"
    - path: "src/store/store.ts"
      provides: "Placeholder file for Phase 2 implementation"
    - path: ".env.development"
      provides: "Dev env vars"
      contains: "VITE_BGG_API_BASE=/bggapi"
    - path: ".env.production"
      provides: "Prod env vars template"
      contains: "VITE_BGG_API_BASE="
  key_links:
    - from: "vite.config.ts"
      to: "@vitejs/plugin-react"
      via: "import react from '@vitejs/plugin-react'"
      pattern: "plugin-react"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "import tailwindcss from '@tailwindcss/vite'"
      pattern: "tailwindcss/vite"
    - from: "vitest.config.ts"
      to: "src/engine/rankingEngine.test.ts"
      via: "include: ['src/**/*.test.ts']"
      pattern: "include.*test.ts"
---

<objective>
Bootstrap the BGG Ranker project: install all dependencies, establish the full folder structure, create a minimal working Vite app, and configure Vitest. Phase 2 begins coding immediately with no dependency setup or reorganization needed.

Purpose: Every subsequent plan (engine tests, proxy config, Firebase deploy) depends on this scaffold existing. This is the foundation all other plans build on.
Output: Working Vite dev server, installed dependencies, folder structure matching SKELETON.md, vitest config ready for engine tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite project and install all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, tsconfig.node.json, index.html, src/main.tsx, src/App.tsx, src/vite-env.d.ts, .gitignore</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md — Standard Stack section (exact package names and versions), Installation commands block, Pattern 4 (vitest.config.ts structure).
    CLAUDE.md — Tech stack list (D-01), folder structure (D-02), minimal App.tsx requirement (D-03).
  </read_first>
  <action>
    Run `npm create vite@latest . -- --template react-ts` in the project root to scaffold the base Vite + React + TypeScript project. Accept overwriting existing files.

    Then install all additional dependencies in one pass from the project root:
    - Production deps: `npm install zustand @tanstack/react-query fast-xml-parser tailwindcss @tailwindcss/vite`
    - Dev deps: `npm install -D vitest@4 @testing-library/react @testing-library/jest-dom @types/node`

    After installation, modify `src/App.tsx` to contain ONLY a function component that returns a single `<h1>BGG Ranker</h1>` element — no boilerplate CSS imports, no counter logic, no default Vite content. Remove the auto-generated `src/App.css` and `src/assets/` directory.

    Modify `src/index.css` to contain only `@import 'tailwindcss';` (Tailwind v4 CSS-first config — no `tailwind.config.js` needed).

    Modify `src/main.tsx` to import `./index.css` and render `<App />` into `document.getElementById('root')`. Keep React 19 style (no StrictMode wrapper required, but acceptable if present).

    Update `tsconfig.json` to add `"types": ["vitest/globals"]` inside `compilerOptions` so TypeScript recognizes `describe`, `it`, `expect` globals without explicit imports in test files.

    Add to `.gitignore`: `dist/`, `node_modules/`, `.env.local`, `.env.*.local`, `proxy/functions/lib/`.
  </action>
  <verify>
    <automated>cd /d/dev/repo/bgg-ranker && npm run dev -- --port 5173 &amp; sleep 4 &amp;&amp; curl -s http://localhost:5173 | grep -c "BGG Ranker" | grep -v "^0$" &amp;&amp; kill %1</automated>
  </verify>
  <acceptance_criteria>
    - `npm list react` shows `react@19.x.x`
    - `npm list zustand` shows `zustand@5.x.x`
    - `npm list vitest` shows `vitest@4.x.x`
    - `npm list tailwindcss` shows `tailwindcss@4.x.x`
    - `npm list @tanstack/react-query` shows `@tanstack/react-query@5.x.x`
    - `npm list fast-xml-parser` shows `fast-xml-parser@5.x.x`
    - `src/App.tsx` contains the string "BGG Ranker" and nothing else substantive
    - `src/index.css` contains `@import 'tailwindcss'`
    - `tsconfig.json` compilerOptions contains `"types": ["vitest/globals"]`
    - `.gitignore` contains `proxy/functions/lib/`
    - `npm run build` exits 0 (production build succeeds)
  </acceptance_criteria>
  <done>Vite dev server starts, renders "BGG Ranker", all dependencies installed and resolvable, TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vitest, create folder structure, env files, and placeholder source files</name>
  <files>vitest.config.ts, .env.development, .env.production, src/api/bggClient.ts, src/store/store.ts, proxy/README.md</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md — Pattern 4 (Vitest Config), Recommended Project Structure section, VITE_BGG_API_BASE env var pattern, Architecture Patterns section.
    CLAUDE.md — D-02 (folder structure), D-05 (VITE_BGG_API_BASE), D-09 (rankingEngine.ts location).
    .planning/phases/01-foundation/01-VALIDATION.md — Test Infrastructure section (environment: 'node', globals: true, include pattern).
  </read_first>
  <action>
    Create `vitest.config.ts` at the project root with `defineConfig` from `vitest/config`. Set `test.environment` to `'node'`, `test.globals` to `true`, and `test.include` to `['src/**/*.test.ts']`. Do NOT extend from `vite.config.ts` — this is a standalone config per Pattern 4 in RESEARCH.md.

    Create `.env.development` with a single line: `VITE_BGG_API_BASE=/bggapi`

    Create `.env.production` with a single line: `VITE_BGG_API_BASE=` (the value is intentionally empty — user fills it with the Firebase Function URL after deploying in Task sequence of Plan 04).

    Create the full folder structure matching SKELETON.md. For directories that will only have real code in Phase 2, create placeholder files:
    - `src/api/bggClient.ts`: export a single empty placeholder comment `// Phase 2: BGG API client implementation` and one exported const `BGG_API_BASE = import.meta.env.VITE_BGG_API_BASE as string` so TypeScript resolves the env var usage
    - `src/store/store.ts`: export a single empty placeholder comment `// Phase 2: Zustand store implementation`
    - `proxy/README.md`: document that after Firebase deploy the executor must copy the Function URL into `.env.production` as `VITE_BGG_API_BASE=https://<region>-<project>.cloudfunctions.net/bgg`

    Create `proxy/functions/` directory (the Firebase Function source — files created in Plan 04).

    Ensure `src/engine/` directory exists (rankingEngine.ts created in Plan 02).
    Ensure `src/components/` directory exists (empty for Phase 1).
  </action>
  <verify>
    <automated>cd /d/dev/repo/bgg-ranker && npx vitest run --reporter=verbose 2>&1 | grep -E "(No test files|passed|failed)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `environment: 'node'` and `globals: true` and `include: ['src/**/*.test.ts']`
    - `.env.development` contains exactly `VITE_BGG_API_BASE=/bggapi`
    - `.env.production` exists (content: `VITE_BGG_API_BASE=` with empty value)
    - `src/api/bggClient.ts` exists and exports `BGG_API_BASE`
    - `src/store/store.ts` exists with placeholder comment
    - `proxy/functions/` directory exists
    - `src/engine/` directory exists
    - `src/components/` directory exists
    - `npx vitest run` exits 0 (no test files found is acceptable — no errors means config is valid)
    - `proxy/README.md` documents the Firebase Function URL format
  </acceptance_criteria>
  <done>Full folder structure established, Vitest configured, env files created. Phase 2 can begin immediately with no setup work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → npm registry | npm install fetches packages over the network; supply chain risk |
| env file → Vite build | VITE_* env vars are embedded in the browser bundle; only non-secret values allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.production` | mitigate | File contains only `VITE_BGG_API_BASE=` (no secret value in Phase 1); user fills value post-deploy; `.env.local` is gitignored |
| T-01-02 | Information Disclosure | `VITE_*` vars in Vite build | accept | `VITE_BGG_API_BASE` is a URL, not a secret; Firebase Function URL is public by design (CORS proxy for a public API) |
| T-01-03 | Tampering | `SessionState` Zustand slice | mitigate | Placeholder store.ts comment explicitly notes Phase 2 must use `partialize` to exclude `SessionState` from `persist` middleware (AUTH-03 scaffolded here) |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | All packages verified via Package Legitimacy Audit in RESEARCH.md — all Approved; no [ASSUMED] or [SUS] packages |
</threat_model>

<verification>
After both tasks complete:
- `npm run dev` starts without errors
- Browser (or curl) shows "BGG Ranker" at localhost:5173
- `npm run build` exits 0
- `npx vitest run` exits 0 (zero tests, zero failures — config valid)
- `cat .env.development` shows `VITE_BGG_API_BASE=/bggapi`
- `cat vitest.config.ts` contains `environment: 'node'` and `globals: true`
- Directory tree shows `src/engine/`, `src/api/`, `src/store/`, `src/components/`, `proxy/functions/`
</verification>

<success_criteria>
Vite dev server runs and renders "BGG Ranker". All 10+ Phase 1 dependencies installed and resolvable. Vitest configured with node environment and globals. Full folder structure per SKELETON.md established. Env files created. Phase 2 can start immediately with zero setup.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
