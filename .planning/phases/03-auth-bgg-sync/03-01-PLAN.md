---
phase: 03-auth-bgg-sync
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/api/bggClient.test.ts
  - src/store/store.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SYNC-01
  - SYNC-02
  - SYNC-03

must_haves:
  truths:
    - "All bggLogin and bggRateGame test cases exist and fail RED before any implementation"
    - "All store action tests (login, startSync, markGameSynced, completeSyncAll, reAuthAndResume, cancelSync) exist and fail RED"
    - "Test for sessionId absence from localStorage partialize output exists and fails RED"
    - "Test for syncedGameIds skip-on-resume exists and fails RED"
    - "Test for beforeunload predicate (comparisonsTotal > comparisonsAtLastSync) exists and fails RED"
  artifacts:
    - path: "src/api/bggClient.test.ts"
      provides: "bggLogin and bggRateGame describe blocks appended to existing file"
      contains: "bggLogin"
    - path: "src/store/store.test.ts"
      provides: "New test file covering login, sync loop, partialize, syncedGameIds, beforeunload predicate"
      exports: []
  key_links:
    - from: "src/api/bggClient.test.ts"
      to: "src/api/bggClient.ts"
      via: "imports bggLogin, bggRateGame (not yet exported)"
      pattern: "import.*bggLogin.*bggRateGame"
    - from: "src/store/store.test.ts"
      to: "src/store/store.ts"
      via: "createAppStore factory"
      pattern: "createAppStore"
---

<objective>
Wave 0: Write all failing RED tests for Phase 3 before any implementation exists.

Purpose: TDD anchor — tests define the exact contracts bggLogin, bggRateGame, and store actions must satisfy. Green plans (03-02 through 03-04) implement against these tests.
Output: Extended bggClient.test.ts + new store.test.ts, all tests failing with import/export errors or assertion failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-auth-bgg-sync/03-CONTEXT.md
@.planning/phases/03-auth-bgg-sync/03-RESEARCH.md
@.planning/STATE.md

<interfaces>
<!-- Existing exports in src/api/bggClient.ts (executor must NOT add these yet — test first) -->
From src/api/bggClient.ts:
  export const BGG_API_BASE: string
  export interface RawGame { id, name, yearPublished, thumbnail }
  export function parseCollectionXml(xmlText: string): RawGame[]
  export function mergeCollections(owned: RawGame[], ratedUnowned: RawGame[]): RawGame[]
  export async function poll202Loop(url: string): Promise<string>
  export async function fetchCollection(username: string): Promise<RawGame[]>

<!-- Phase 3 additions that do NOT exist yet — tests import them to force RED -->
  export async function bggLogin(username: string, password: string): Promise<{ sessionId: string }>
  export async function bggRateGame(gameId: string, ratingInt: number, sessionId: string): Promise<void>

<!-- Existing exports in src/store/store.ts -->
From src/store/store.ts:
  export interface Game { id, name, yearPublished, thumbnail }
  export type AppStore = SessionStateSlice & CollectionStateSlice & RankingsStateSlice & ComparisonStateSlice & AppActions
  export function selectRandomPair(ratings, skipQueue): [string,string]|null
  export function createAppStore(rawStorage: StateStorage): UseBoundStore<StoreApi<AppStore>>
  export const useStore: UseBoundStore<StoreApi<AppStore>>
  -- partialize currently persists: games, lastFetched, ratings, comparisonsTotal, rankingsUsername, version

<!-- Phase 3 store shape additions (not yet in store.ts — tests reference them to force RED) -->
SessionStateSlice gains: sessionId: string | null
RankingsStateSlice gains: syncedGameIds: string[], comparisonsAtLastSync: number
ComparisonStateSlice.view gains: 'syncing'
AppActions gains: login(username, password), startSync(), markGameSynced(gameId), completeSyncAll(), reAuthAndResume(password), cancelSync()
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED tests for bggLogin and bggRateGame (AUTH-01, SYNC-01)</name>
  <files>src/api/bggClient.test.ts</files>
  <read_first>
    - src/api/bggClient.test.ts (read full file — append new describe blocks after existing ones, matching style exactly)
    - src/api/bggClient.ts (understand existing delay helper, BGG_API_BASE, error throw conventions)
    - .planning/phases/03-auth-bgg-sync/03-RESEARCH.md (Pattern 1: bggLogin, Pattern 2: bggRateGame — exact endpoint, body format, error shape)
  </read_first>
  <behavior>
    RED phase only — import bggLogin and bggRateGame from ./bggClient. They do not exist yet; the import will cause RED.

    bggLogin describe block (AUTH-01):
    - Test: resolves {sessionId: "abc123"} when fetch returns 200 with body {sessionId: "abc123"}
    - Test: throws Error containing "BGG login failed" when fetch returns 401
    - Test: throws Error containing "no sessionId" when fetch returns 200 with empty body {}

    bggRateGame describe block (SYNC-01):
    - Test: calls fetch with POST to BGG_API_BASE + "/api/geekrating"
    - Test: sends X-BGG-Session header equal to the sessionId argument
    - Test: sends ratingInt/100 as the rating field — e.g. ratingInt=743 sends rating="7.43" (float conversion at call site only, per D-10)
    - Test: sends objectid=gameId and objecttype=thing as form fields
    - Test: resolves (void) on 200 OK
    - Test: throws with .status===401 when fetch returns 401 (caller interprets this as session-expired per D-18)
    - Test: throws with .status===500 when fetch returns 500

    Each test uses vi.useFakeTimers() and vi.stubGlobal('fetch', vi.fn()) in beforeEach/afterEach matching existing poll202Loop test pattern.
  </behavior>
  <action>
    Append two new describe blocks to the bottom of src/api/bggClient.test.ts after the existing fetchCollection describe block.

    Import bggLogin and bggRateGame by adding them to the existing named import from ./bggClient at the top of the file. Because bggClient.ts does not export them yet, the test file will fail to compile (RED). Do not touch any existing test code.

    bggLogin tests: mock fetch with vi.mocked(fetch).mockResolvedValueOnce(). Simulate BGG_API_BASE by using an empty string or leaving BGG_API_BASE as import.meta.env.VITE_BGG_API_BASE (the stub returns the value regardless of URL). For the "no sessionId" test, mock response body as JSON.stringify({}) with ok:true status:200.

    bggRateGame tests: mock fetch similarly. For status-carrying throw tests, use expect(err).toMatchObject({status: 401}). Use URLSearchParams parsing on the captured request body string to assert individual form fields. Access the captured Headers via the second argument to fetch mock: mockFetch.mock.calls[0][1].headers — check 'X-BGG-Session' key.

    Do NOT add implementation — only tests. Do NOT run npm test yet; the task is complete when the file is saved.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | grep -E "(bggLogin|bggRateGame|FAIL|Cannot find|not exported)"</automated>
  </verify>
  <done>bggClient.test.ts has two new describe blocks for bggLogin and bggRateGame; running npm test shows these tests as failing (import error or assertion failure — RED confirmed)</done>
</task>

<task type="tdd">
  <name>Task 2: RED tests for store Phase 3 actions and partialize (AUTH-01 through SYNC-03)</name>
  <files>src/store/store.test.ts</files>
  <read_first>
    - src/store/store.ts (full file — createAppStore factory pattern, existing actions, current partialize allowlist at lines 304-313, SessionStateSlice shape)
    - .planning/phases/03-auth-bgg-sync/03-CONTEXT.md (D-11: syncedGameIds semantics; D-12: comparisonsAtLastSync; D-13: sessionId ephemeral; D-14: persisted fields; D-16: action list)
    - .planning/phases/03-auth-bgg-sync/03-RESEARCH.md (Pattern 3: startSync loop; Pattern 5: partialize extension; Pitfall 3: AUTH-03 partialize regression)
  </read_first>
  <behavior>
    Create src/store/store.test.ts from scratch. Use createAppStore with a createMockStorage() helper (in-memory Map) matching the pattern implied by the factory signature.

    Describe: "login action (AUTH-01)"
    - Test: login() sets sessionId in store state
    - Test: sessionId is absent from the partialize output written to storage (AUTH-03 — run login, read storage, parse JSON, assert no sessionId key in state object)
    - Test: login() sets view to 'loading' during execution

    Describe: "startSync action (SYNC-01, SYNC-02)"
    - Test: startSync() calls bggRateGame for each game in ratings (mock bggRateGame)
    - Test: startSync() skips gameIds already in syncedGameIds (SYNC-03 resume anchor)
    - Test: startSync() increments syncProgress after each successful write
    - Test: startSync() sets syncStatus to 'session-expired' when bggRateGame throws with status 401

    Describe: "markGameSynced action (SYNC-03)"
    - Test: markGameSynced('123') appends '123' to syncedGameIds
    - Test: markGameSynced() increments syncProgress

    Describe: "completeSyncAll action (SYNC-03)"
    - Test: completeSyncAll() clears syncedGameIds to []
    - Test: completeSyncAll() sets comparisonsAtLastSync = comparisonsTotal

    Describe: "reAuthAndResume action (AUTH-03)"
    - Test: reAuthAndResume() calls bggLogin and updates sessionId
    - Test: reAuthAndResume() resumes sync from last position (syncedGameIds not cleared)

    Describe: "cancelSync action"
    - Test: cancelSync() sets sessionId to null (loop check aborts)
    - Test: cancelSync() does NOT clear syncedGameIds (preserve for resume per Q2 resolution)

    Describe: "beforeunload predicate (AUTH-02)"
    - Test: comparisonsTotal > comparisonsAtLastSync is true after a pick with comparisonsAtLastSync=0
    - Test: comparisonsTotal === comparisonsAtLastSync after completeSyncAll()

    Describe: "RankingsStateSlice persistence (SYNC-03)"
    - Test: syncedGameIds and comparisonsAtLastSync are present in partialize output
    - Test: sessionId is absent from partialize output (redundant with AUTH-01 test — belt and suspenders)

    Mock bggLogin and bggRateGame using vi.mock('../api/bggClient', ...). The mock is needed because store.ts imports bggClient directly — vi.mock at the top of the test file intercepts the import.
  </behavior>
  <action>
    Create src/store/store.test.ts as a new file.

    Helper createMockStorage(): returns an object with getItem/setItem/removeItem backed by a new Map(). This matches the StateStorage interface required by createAppStore.

    At the top of the test file: vi.mock('../api/bggClient', () => ({ bggLogin: vi.fn(), bggRateGame: vi.fn(), fetchCollection: vi.fn(), BGG_API_BASE: '/bggapi' })). This makes vi.mocked(bggLogin) etc. available in tests.

    Each test creates a fresh store via createAppStore(createMockStorage()). Call .getState() to read state. Dispatch actions via store.getState().actionName(args).

    For async actions (login, startSync, reAuthAndResume): await them. For startSync tests: make bggRateGame return resolved void by default; make it throw for the 401 test using vi.fn().mockRejectedValueOnce(Object.assign(new Error('401'), {status: 401})).

    For partialize tests: after dispatching login, call storage.getItem('bgg-ranker:v1:collection-and-rankings'), JSON.parse it, and assert on the state object's keys.

    Do NOT add any implementation to store.ts. The tests will fail because: (a) bggLogin/bggRateGame don't exist in bggClient.ts yet, (b) login/startSync/etc. don't exist in store.ts yet, (c) sessionId/syncedGameIds/comparisonsAtLastSync fields don't exist on the store. All failures are RED confirmations.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | grep -E "(store.test|FAIL|login|startSync|Cannot find)"</automated>
  </verify>
  <done>src/store/store.test.ts exists with all describe blocks; npm test output shows these tests failing RED — actions or fields not found on store</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Source | Tests import not-yet-exported symbols — compile errors confirm RED state |
| Test → Storage | Mock storage in-memory Map; no real localStorage touched in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 3; no install steps in this plan |
| T-03-01 | Information Disclosure | partialize test | mitigate | Test explicitly asserts sessionId absent from localStorage output (AUTH-03 belt-and-suspenders) |
</threat_model>

<verification>
npm test exits with failures (RED) — all new test cases fail, no existing tests regress.
Existing bggClient describe blocks (parseCollectionXml, mergeCollections, poll202Loop, fetchCollection) remain green.
</verification>

<success_criteria>
- src/api/bggClient.test.ts contains describe blocks for bggLogin (3 tests) and bggRateGame (7 tests)
- src/store/store.test.ts exists with 8 describe blocks covering all Phase 3 actions and partialize contracts
- npm test output shows new tests as FAIL with import/type errors or assertion failures — RED confirmed
- No previously-passing test goes from green to red
</success_criteria>

<output>
Create .planning/phases/03-auth-bgg-sync/03-01-SUMMARY.md when done
</output>
