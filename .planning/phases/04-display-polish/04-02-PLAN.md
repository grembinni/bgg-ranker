---
phase: 04-display-polish
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/store/store.ts
autonomous: true
requirements:
  - DISP-02

must_haves:
  truths:
    - "pick() detects when the winner was ranked lower than the loser and sets lastUpset"
    - "lastUpset is cleared after 5 seconds by a module-level timer"
    - "Rapid picks cancel the previous timer before setting a new one"
    - "lastUpset is NOT persisted to localStorage"
    - "login() auto-resumes when rankingsUsername matches and ratings/games exist"
    - "login() calls fetchCollection when username differs or no stored rankings"
    - "logout() clears sessionId/sessionUsername and sets view to entry without clearing rankings"
    - "logout() calls cancelSync() before clearing session state"
  artifacts:
    - path: "src/store/store.ts"
      provides: "lastUpset field, upsetTimer, extended pick(), extended login(), new logout(), cleaned fetchCollection()"
      contains: "lastUpset"
    - path: "src/store/store.ts"
      provides: "logout action in AppActions interface"
      contains: "logout.*void"
  key_links:
    - from: "src/store/store.ts pick()"
      to: "applyUpset"
      via: "ranked array built from pre-upset ratings BEFORE applyUpset is called"
      pattern: "ranked.*sort.*applyUpset|applyUpset.*ranked.*sort"
    - from: "src/store/store.ts login()"
      to: "continueSession()"
      via: "PERSIST-02 guard checked BEFORE fetchCollection call"
      pattern: "rankingsUsername.*username.*continueSession"
    - from: "src/store/store.ts logout()"
      to: "cancelSync()"
      via: "cancelSync called first (idempotent)"
      pattern: "cancelSync.*sessionId.*null"
---

<objective>
Wave 1a: Extend store.ts with all Phase 4 state and action changes.

Purpose: Provide the data layer that ComparisonView and the test suite need. No UI changes in this plan — pure store surgery. After this plan, the store tests from 04-01 turn GREEN.

Output: store.ts with lastUpset field in ComparisonStateSlice, upsetTimer module-level variable, extended pick(), extended login() with auto-resume, new logout() action, cleaned fetchCollection() (PERSIST-02 guard removed from there, now lives in login()).
</objective>

<execution_context>
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-CONTEXT.md
</execution_context>

<context>
@D:\dev\repo\bgg-ranker\.planning\PROJECT.md
@D:\dev\repo\bgg-ranker\.planning\ROADMAP.md
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-CONTEXT.md
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/store/store.ts — ComparisonStateSlice (lines 63-73):
Current interface fields: view, currentPair, sessionComparisons, skipQueue, loadingMessage,
errorMessage, syncStatus, syncProgress, syncTotal.
ADD: lastUpset: { winnerName: string; spotsGained: number } | null

From src/store/store.ts — AppActions (lines 75-96):
Current actions include cancelSync(): void at line 95.
ADD after cancelSync: logout(): void

From src/store/store.ts — module level (line 152):
let completeSyncTimer: ReturnType<typeof setTimeout> | null = null
ADD directly below: let upsetTimer: ReturnType<typeof setTimeout> | null = null

From src/store/store.ts — initial state (lines 173-193):
syncTotal: 0 is the last field before actions.
ADD after syncTotal: 0: lastUpset: null,

From src/store/store.ts — resetForNewUser() (lines 316-333):
First line: if (completeSyncTimer) { clearTimeout(completeSyncTimer); completeSyncTimer = null }
ADD after that line: if (upsetTimer) { clearTimeout(upsetTimer); upsetTimer = null }

From src/store/store.ts — pick() (lines 335-353):
Current: const { ratings, comparisonsTotal, skipQueue, sessionComparisons, dirtyGameIds } = get()
Must change to: also destructure games.

From src/store/store.ts — login() (lines 423-438):
Current: after bggLogin succeeds, sets sessionId then calls fetchCollection(username) unconditionally.
Must add PERSIST-02 check BEFORE fetchCollection call.
Also add: set({ sessionId: result.sessionId, sessionUsername: username }) — note: current code does NOT set sessionUsername in login(); add it here.

From src/store/store.ts — fetchCollection() (lines 201-213):
PERSIST-02 guard at lines 204-213 must be REMOVED. The guard's purpose now lives in login().
After removal, fetchCollection always proceeds to fetch — no more "continue?" redirect from collection fetch.

From src/store/store.ts — cancelSync() (lines 532-537):
Sets sessionId=null, view='comparison', syncStatus='idle'. This is the action logout() must call first.

From src/store/store.ts — partialize (lines 542-554):
Must NOT add lastUpset here. Verified: lastUpset is session-only per D-03.

From src/engine/rankingEngine.ts — applyUpset position pattern (lines 172-178):
const ranked = Object.entries(ratings).sort((a, b) => b[1] - a[1])
const winnerPos = ranked.findIndex(([id]) => id === winnerId)
const loserPos = ranked.findIndex(([id]) => id === loserId)
This pattern must be replicated in pick() BEFORE calling applyUpset (Pitfall 2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lastUpset to store interface, initial state, and upsetTimer module variable</name>
  <files>src/store/store.ts</files>
  <read_first>
    - src/store/store.ts (full file — establish full context before editing; do NOT skip this)
    - .planning/phases/04-display-polish/04-CONTEXT.md (D-03: session-only, not persisted)
    - .planning/phases/04-display-polish/04-PATTERNS.md (Pattern 1: module-level timer)
  </read_first>
  <behavior>
    - ComparisonStateSlice has lastUpset: { winnerName: string; spotsGained: number } | null
    - Initial state has lastUpset: null
    - Module-level variable upsetTimer: ReturnType<typeof setTimeout> | null = null declared after completeSyncTimer
    - AppActions interface has logout(): void added after cancelSync(): void
    - partialize does NOT include lastUpset (no change to partialize)
  </behavior>
  <action>
    Make four targeted edits to store.ts:

    1. In ComparisonStateSlice interface (after syncTotal: number line):
       Add field: lastUpset: { winnerName: string; spotsGained: number } | null

    2. In initial state object (after syncTotal: 0 line):
       Add: lastUpset: null,

    3. After line 152 (let completeSyncTimer: ...):
       Add: let upsetTimer: ReturnType<typeof setTimeout> | null = null

    4. In AppActions interface (after cancelSync(): void):
       Add: logout(): void

    Do NOT modify partialize. Do NOT add lastUpset anywhere in the persist options object.
    TypeScript should compile without errors after this task (new field with null initial value satisfies the type).
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/store/store.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    store.ts compiles (npx tsc --noEmit exits 0).
    ComparisonStateSlice contains lastUpset type signature.
    AppActions contains logout(): void.
    Existing store tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend pick(), login(), fetchCollection(); add logout() and upsetTimer cancel in resetForNewUser()</name>
  <files>src/store/store.ts</files>
  <read_first>
    - src/store/store.ts (already read in Task 1; re-read lines 200-440 for the specific action bodies before editing)
    - .planning/phases/04-display-polish/04-PATTERNS.md (Pattern 1 timer, Pattern 5 login auto-resume)
    - .planning/phases/04-display-polish/04-RESEARCH.md (Pitfall 1 timer leak, Pitfall 2 wrong positions, Pitfall 3 fetchCollection conflict, Anti-patterns)
  </read_first>
  <behavior>
    pick(winnerId, loserId):
    - Builds const ranked = Object.entries(ratings).sort((a, b) => b[1] - a[1]) from pre-upset ratings
    - Computes winnerPos and loserPos using findIndex BEFORE calling applyUpset
    - If winnerPos > loserPos AND both !== -1: upset detected
    - Cancels any previous upsetTimer with clearTimeout before setting new one
    - Sets upsetTimer = setTimeout(clear lastUpset, 5000)
    - Includes lastUpset: newLastUpset in the final set() call
    - If no upset: lastUpset: null in the final set() call (clears any stale callout from previous pick)
    - Destructures games from get() to look up winner name

    resetForNewUser():
    - Adds upsetTimer cancellation after existing completeSyncTimer cancellation

    login(username, password) — extended after bggLogin succeeds:
    - Sets sessionId: result.sessionId AND sessionUsername: username in the same set() call
    - Checks: if state.rankingsUsername === username AND ratings non-empty AND games non-empty → continueSession()
    - Else: sets loadingMessage 'Fetching your games…' then calls fetchCollection(username)

    fetchCollection(username) — PERSIST-02 guard removed:
    - Remove the block at lines 204-213 (the guard that returned early and set view: 'entry')
    - fetchCollection always proceeds to fetch after this change

    logout() — new action (add after cancelSync()):
    - Calls get().cancelSync() first (idempotent; aborts in-flight sync loop by nulling sessionId)
    - Then: set({ sessionId: null, sessionUsername: null, view: 'entry' })
    - Does NOT touch ratings, games, rankingsUsername, dirtyGameIds, or any persisted fields
  </behavior>
  <action>
    Edit src/store/store.ts to make the following changes. Read the file first, then apply edits:

    A. pick() — replace the current body (lines 335-353) with the extended version:
       Destructure games alongside existing fields.
       Build ranked array from Object.entries(ratings).sort((a, b) => b[1] - a[1]).
       Compute winnerPos = ranked.findIndex(([id]) => id === winnerId).
       Compute loserPos = ranked.findIndex(([id]) => id === loserId).
       Call applyUpset as before.
       Compute newLastUpset: if winnerPos > loserPos AND both !== -1, build { winnerName: games[winnerId]?.name ?? winnerId, spotsGained: winnerPos - loserPos }, cancel previous upsetTimer, set new 5s upsetTimer. Otherwise newLastUpset = null.
       Include lastUpset: newLastUpset in the final set() call.

    B. resetForNewUser() — add upsetTimer cancellation:
       After the existing completeSyncTimer null-check line, add: if (upsetTimer) { clearTimeout(upsetTimer); upsetTimer = null }

    C. login() — replace the try block body after bggLogin succeeds:
       Change set({ sessionId: result.sessionId, ... }) to include sessionUsername: username.
       Add PERSIST-02 check using get() immediately after (not before bggLogin, because bggLogin is async).
       Call get().continueSession() on match; else set loadingMessage and call fetchCollection.

    D. fetchCollection() — remove PERSIST-02 guard:
       Delete the early-return block that checks rankingsUsername === username && ratings non-empty && games non-empty and sets view: 'entry'.

    E. After cancelSync() action body, add logout() action body as described in behavior.

    CRITICAL PITFALL GUARD — pick(): compute ranked array BEFORE the applyUpset() call, not after.
    CRITICAL PITFALL GUARD — login(): check PERSIST-02 condition from state = get() AFTER setting sessionId (so if caller checks sessionId in continueSession, it is set). But note: the guard checks rankingsUsername/ratings/games — none of which change during login. Order: set sessionId first, then check guard.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/store/store.test.ts 2>&1 | tail -40</automated>
  </verify>
  <done>
    All store.test.ts tests pass (GREEN), including the new describe blocks from 04-01.
    Specifically:
      - pick() upset detection tests: lastUpset set when winnerPos > loserPos
      - pick() timer test: lastUpset cleared after 5s with fake timers
      - login() auto-resume tests: continueSession() called for matching user, fetchCollection() for others
    npx tsc --noEmit exits 0.
    Commit: "feat(04-02): extend store — lastUpset, pick upset detection, login auto-resume, logout"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store.login() → bggLogin() | Credentials leave client boundary; result (sessionId) stored in Zustand session-only state |
| store.logout() → Zustand state | Clears session fields; persisted rankings NOT cleared per D-08 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | lastUpset in partialize | mitigate | lastUpset excluded from partialize — session-only per D-03; no PII in callout text (game name + integer) |
| T-04-02 | Tampering | logout() clears session | accept | logout() does not clear rankings; intentional per D-08; re-login auto-resume is the recovery path |
| T-04-03 | Denial of Service | upsetTimer leak on rapid picks | mitigate | clearTimeout(upsetTimer) before every new setTimeout per Pitfall 1 in RESEARCH.md |
| T-04-SC | Tampering | npm installs | accept | No new packages; zero install surface |
</threat_model>

<verification>
npm test -- src/store/store.test.ts

All describe blocks green including:
  - 'pick() upset detection (D-01, D-02, D-03)'
  - 'login() auto-resume (D-07)'
  - All previously-passing fetchCollection, selectRandomPair, pick action tests

npx tsc --noEmit (TypeScript compile check — no errors)
</verification>

<success_criteria>
- store.ts has lastUpset: ... | null in ComparisonStateSlice interface
- store.ts has logout(): void in AppActions interface
- store.ts has upsetTimer module-level variable
- pick() computes ranked from pre-upset ratings before calling applyUpset
- login() checks PERSIST-02 before calling fetchCollection
- fetchCollection() no longer has early-return PERSIST-02 guard
- logout() calls cancelSync() then clears session fields only
- lastUpset absent from partialize
- All store.test.ts tests green
</success_criteria>

<output>
Create .planning/phases/04-display-polish/04-02-SUMMARY.md when done
</output>