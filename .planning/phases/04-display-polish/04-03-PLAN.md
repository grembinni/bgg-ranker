---
phase: 04-display-polish
plan: 03
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/components/GameCard.tsx
autonomous: true
requirements:
  - DISP-01

must_haves:
  truths:
    - "Each game card shows a 192px-tall image (h-48) during comparison"
    - "The image is wrapped in a link to boardgamegeek.com/boardgame/{game.id}"
    - "When a game has no thumbnail URL, a gray placeholder div with 'No image' text is shown"
    - "The rank display shows only '#N' without the total game count"
  artifacts:
    - path: "src/components/GameCard.tsx"
      provides: "Upgraded card with h-48 image or placeholder, BGG link, #N rank"
      contains: "h-48"
    - path: "src/components/GameCard.tsx"
      provides: "No reference to totalGames variable after upgrade"
      contains: "boardgamegeek.com/boardgame"
  key_links:
    - from: "src/components/GameCard.tsx img"
      to: "https://boardgamegeek.com/boardgame/{game.id}"
      via: "anchor wrapping the img element"
      pattern: "href.*boardgamegeek\\.com/boardgame"
    - from: "src/components/GameCard.tsx"
      to: "game.thumbnail"
      via: "ternary: thumbnail ? img-in-anchor : placeholder-div"
      pattern: "game\\.thumbnail \\?"
---

<objective>
Wave 1a (parallel with 04-02): Upgrade GameCard.tsx with the DISP-01 cover art requirements.

Purpose: Show cover art at h-48 with a BGG page link, and display a gray placeholder when no thumbnail URL exists. Remove the "of N total" rank suffix. This plan is self-contained — it only touches GameCard.tsx and has no file overlap with 04-02.

Output: GameCard.tsx renders a 192px image wrapped in a BGG link (or a gray placeholder), and displays rank as "#N" only.
</objective>

<execution_context>
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-CONTEXT.md
</execution_context>

<context>
@D:\dev\repo\bgg-ranker\.planning\PROJECT.md
@D:\dev\repo\bgg-ranker\.planning\ROADMAP.md
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-CONTEXT.md
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/GameCard.tsx (full file, 52 lines):

GameCardProps: { gameId: string, onPick: () => void, onMarkUnplayed: () => void }
Game interface (from store.ts): { id: string, name: string, yearPublished: number, thumbnail: string, userRating?: number | null }

Current thumbnail block (lines 33-39):
  {game.thumbnail && (
    <img
      src={game.thumbnail.startsWith('//') ? `https:${game.thumbnail}` : game.thumbnail}
      alt={game.name}
      className="w-full h-32 object-contain"
    />
  )}
Replace entirely with the ternary pattern from D-06.

Current rank display (line 42):
  <div className="text-sm text-gray-500">#{rank} of {totalGames}</div>
Replace with: <div className="text-sm text-gray-500">#{rank}</div>
Also remove: const totalGames = Object.keys(ratings).length (line 20) — no longer needed.

BGG thumbnail URLs from the API often start with '//' (protocol-relative). The existing
`game.thumbnail.startsWith('//') ? 'https:' + game.thumbnail : game.thumbnail` logic
must be preserved inside the img src attribute.

D-06 exact Tailwind classes:
  img: "w-full h-48 object-contain aspect-square"
  placeholder div: "h-48 bg-gray-100 rounded flex items-center justify-center text-gray-400 text-sm aspect-square"
  anchor: href="https://boardgamegeek.com/boardgame/{game.id}" target="_blank" rel="noopener noreferrer"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade GameCard thumbnail, link, placeholder, and rank display</name>
  <files>src/components/GameCard.tsx</files>
  <read_first>
    - src/components/GameCard.tsx (full file — read before editing; only 52 lines)
    - .planning/phases/04-display-polish/04-CONTEXT.md (D-06: exact classes, placeholder text, link format)
    - .planning/phases/04-display-polish/04-PATTERNS.md (Pattern 4: thumbnail with placeholder; rank display diff)
  </read_first>
  <behavior>
    - game.thumbnail is a non-empty string → renders: anchor wrapping img at h-48 object-contain aspect-square
    - game.thumbnail is empty string or falsy → renders: placeholder div h-48 bg-gray-100 rounded flex items-center justify-center text-gray-400 text-sm aspect-square containing text "No image"
    - img src handles protocol-relative URLs: game.thumbnail.startsWith('//') ? 'https:' + game.thumbnail : game.thumbnail
    - Anchor: href="https://boardgamegeek.com/boardgame/{game.id}" target="_blank" rel="noopener noreferrer"
    - Rank div shows only "#{rank}" — no "of {totalGames}"
    - const totalGames = ... line is removed (unused after rank change)
    - ratings selector (line 16) remains — it is still needed for getRankPosition
  </behavior>
  <action>
    Edit src/components/GameCard.tsx with three targeted changes:

    1. Remove the const totalGames = Object.keys(ratings).length line (currently line 20).
       Keep const rank = getRankPosition(gameId, ratings) — that line stays.

    2. Replace the thumbnail conditional block (currently lines 33-39):
       Replace the entire {game.thumbnail && (...)} block with a ternary:
         {game.thumbnail ? (
           <a href={`https://boardgamegeek.com/boardgame/${game.id}`} target="_blank" rel="noopener noreferrer">
             <img
               src={game.thumbnail.startsWith('//') ? `https:${game.thumbnail}` : game.thumbnail}
               alt={game.name}
               className="w-full h-48 object-contain aspect-square"
             />
           </a>
         ) : (
           <div className="h-48 bg-gray-100 rounded flex items-center justify-center text-gray-400 text-sm aspect-square">
             No image
           </div>
         )}

    3. Replace the rank display line (currently line 42):
       Old: <div className="text-sm text-gray-500">#{rank} of {totalGames}</div>
       New: <div className="text-sm text-gray-500">#{rank}</div>

    No other changes. getRankPosition function, GameCardProps, useStore calls, buttons — all unchanged.
    Do NOT add imports; no new imports are needed.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/components/ComparisonView.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/GameCard.tsx contains the string "h-48" (verify: grep -c "h-48" src/components/GameCard.tsx outputs 2 — one in img className, one in placeholder div className)
    - src/components/GameCard.tsx contains "boardgamegeek.com/boardgame" in an href attribute
    - src/components/GameCard.tsx does NOT contain "of {totalGames}" or "totalGames"
    - src/components/GameCard.tsx does NOT contain "h-32" (old thumbnail height)
    - ComparisonView.test.tsx describe block 'GameCard thumbnail (DISP-01)' tests pass GREEN after this task
    - npx tsc --noEmit exits 0
    - Commit: "feat(04-03): upgrade GameCard — h-48 thumbnail, BGG link, placeholder, hash-only rank (DISP-01)"
  </acceptance_criteria>
  <done>
    GameCard.tsx renders h-48 image-or-placeholder, BGG anchor link, and "#N" rank.
    ComparisonView.test.tsx thumbnail describe block is GREEN.
    TypeScript compiles without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameCard img src → BGG CDN | Image URL from BGG API (already trusted in prior phases); protocol-relative URLs normalized to https |
| anchor href → boardgamegeek.com | Constructed from game.id (integer-like string from BGG API); target="_blank" + rel="noopener noreferrer" prevents tab-napping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | anchor href constructed from game.id | accept | game.id is a BGG-assigned identifier fetched from API in a prior authenticated session; not user-supplied input in this phase |
| T-04-05 | Information Disclosure | target="_blank" tab-napping | mitigate | rel="noopener noreferrer" present on all external anchors per D-06 |
| T-04-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm test -- src/components/ComparisonView.test.tsx

ComparisonView.test.tsx 'GameCard thumbnail (DISP-01)' describe block: all tests GREEN.
npx tsc --noEmit exits 0.
</verification>

<success_criteria>
- GameCard.tsx renders h-48 image in an anchor to boardgamegeek.com/boardgame/{id} when thumbnail URL exists
- GameCard.tsx renders h-48 gray placeholder div with "No image" text when thumbnail is empty/falsy
- GameCard.tsx rank display is "#N" with no total count
- totalGames variable removed
- ComparisonView.test.tsx DISP-01 describe block tests pass
- TypeScript compiles clean
</success_criteria>

<output>
Create .planning/phases/04-display-polish/04-03-SUMMARY.md when done
</output>