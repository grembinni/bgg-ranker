---
phase: 04-display-polish
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/components/ComparisonView.tsx
  - src/components/UsernameEntry.tsx
autonomous: false
requirements:
  - DISP-01
  - DISP-02

must_haves:
  truths:
    - "The comparison header shows hamburger button (top-left), session counter (center), username (right)"
    - "Clicking the hamburger reveals Sync to BGG, Refresh rankings, and Logout in a dropdown"
    - "Sync to BGG item is disabled when dirtyGameIds is empty or sessionId is null"
    - "Clicking Logout calls store.logout() and does not clear stored rankings"
    - "After picking an upset winner, an amber callout appears between the card grid and action buttons"
    - "The callout shows '[winner name] moved up N spots' (singular: '1 spot')"
    - "The callout disappears when lastUpset is null (no reserved layout slot)"
    - "Standalone Refresh and Sync to BGG buttons are removed from the action bar and header"
    - "UsernameEntry no longer shows a continue-prompt block"
    - "Login form submits username + password and calls login() directly"
  artifacts:
    - path: "src/components/ComparisonView.tsx"
      provides: "Hamburger header, upset callout row, cleaned action bar"
      contains: "lastUpset"
    - path: "src/components/ComparisonView.tsx"
      provides: "Hamburger dropdown with logout"
      contains: "menuOpen"
    - path: "src/components/UsernameEntry.tsx"
      provides: "Simplified entry form with no continue-prompt block"
      contains: "login"
  key_links:
    - from: "src/components/ComparisonView.tsx"
      to: "src/store/store.ts lastUpset"
      via: "useStore(s => s.lastUpset) selector"
      pattern: "lastUpset.*useStore"
    - from: "src/components/ComparisonView.tsx"
      to: "src/store/store.ts logout()"
      via: "useStore(s => s.logout) selector + hamburger click handler"
      pattern: "logout.*useStore"
    - from: "src/components/UsernameEntry.tsx"
      to: "src/store/store.ts login()"
      via: "handleSubmit calls login(trimmed, trimmedPassword)"
      pattern: "login.*trimmed.*trimmedPassword"
---

<objective>
Wave 2: Wire the store changes from 04-02 into ComparisonView and simplify UsernameEntry.

Purpose: Deliver the visible Phase 4 features — hamburger navigation (D-08, D-09), upset callout (D-04, D-05), and cleaned-up login form (D-07). After this plan, all Phase 4 describe blocks in both test files are GREEN and the visual experience matches the CONTEXT.md decisions.

Output: ComparisonView with hamburger header and upset callout; UsernameEntry without the continue-prompt block.
</objective>

<execution_context>
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-CONTEXT.md
</execution_context>

<context>
@D:\dev\repo\bgg-ranker\.planning\PROJECT.md
@D:\dev\repo\bgg-ranker\.planning\ROADMAP.md
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-CONTEXT.md
@D:\dev\repo\bgg-ranker\.planning\phases\04-display-polish\04-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/store/store.ts (after 04-02 completes):
  lastUpset: { winnerName: string; spotsGained: number } | null  — new field in store
  logout(): void  — new action in store
  cancelSync(): void  — existing action (called by logout internally; ComparisonView does NOT need to call it directly)

From src/components/ComparisonView.tsx (current — read before editing):
  Imports: useStore from store, GameCard
  Selectors (lines 5-18): currentPair, sessionComparisons, comparisonsTotal, sessionUsername,
    dirtyGameIds, unplayedIds, pick, skip, refresh, startSync, markUnplayed,
    showRankedList, showUnplayedList, sessionId
  syncDisabled = !sessionId || dirtyGameIds.length === 0
  Header (lines 34-45): username left, counter center, Sync button right — REPLACE entire header
  Grid (lines 46-57): unchanged
  Action buttons (lines 58-87): Skip, Refresh, Ranked list, Unplayed — REMOVE Refresh button

ADD selectors: lastUpset, logout
ADD import: useState from 'react'
ADD local state: const [menuOpen, setMenuOpen] = useState(false)

New header pattern (D-08, D-09):
  Hamburger button (☰ with aria-label="Menu") → toggles menuOpen
  Dropdown (absolutely positioned, left-0 top-full z-10) with three items:
    - "Sync to BGG" button: onClick=handleSync, disabled=syncDisabled, disabled styling
    - "Refresh rankings" button: onClick=handleRefresh
    - "Logout" button: onClick=handleLogout
  Counter span: "{sessionComparisons} this session · {comparisonsTotal} total" (U+00B7 MIDDLE DOT)
  Username span: right side

New upset callout (D-04, D-05):
  Inserted as a sibling AFTER the grid div and BEFORE the action buttons div.
  {lastUpset !== null && (
    <div className="bg-amber-50 border border-amber-200 text-amber-800 rounded px-4 py-2 text-center text-sm mt-4">
      {lastUpset.winnerName} moved up {lastUpset.spotsGained} {lastUpset.spotsGained === 1 ? 'spot' : 'spots'}
    </div>
  )}

Handlers pattern (D-08 — dismiss on action click):
  handleSync = () => { setMenuOpen(false); startSync() }
  handleRefresh = () => { setMenuOpen(false); refresh() }
  handleLogout = () => { setMenuOpen(false); logout() }

From src/components/UsernameEntry.tsx (current):
  showContinuePrompt block (lines 101-123) — DELETE the entire JSX block
  Selectors continueSession, resetForNewUser, sessionUsername, rankingsUsername, ratings — REMOVE these
  Keep: login, useState hooks, handleSubmit, all form JSX
  showContinuePrompt computed variable (lines 17-20) — REMOVE
  ratingsCount variable (line 22) — REMOVE

From ComparisonView.test.tsx (after 04-01 adds new describe blocks):
  Mock object needs to be up-to-date. The existing Sync button tests (old describe block)
  will FAIL after this plan because the standalone Sync button is removed.
  Resolution: Update the existing describe block title and test assertions to point to the
  hamburger Sync item. OR: since the existing tests check for 'Sync to BGG' by role/name,
  and the hamburger item is also named 'Sync to BGG', the tests may still pass if the
  hamburger is open. The safest approach: update the existing describe block to:
    1. Open hamburger first (fireEvent.click(screen.getByRole('button', { name: /menu/i })))
    2. Then query for 'Sync to BGG'
  This is a test file change — add it alongside the ComparisonView component edit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ComparisonView header, add upset callout, remove standalone Refresh/Sync; update test mock and old Sync tests</name>
  <files>src/components/ComparisonView.tsx, src/components/ComparisonView.test.tsx</files>
  <read_first>
    - src/components/ComparisonView.tsx (full file — current state after 04-02/04-03 complete)
    - src/components/ComparisonView.test.tsx (full file — understand mock and all describe blocks before editing)
    - .planning/phases/04-display-polish/04-CONTEXT.md (D-04, D-05, D-08, D-09)
    - .planning/phases/04-display-polish/04-PATTERNS.md (Pattern 2 callout, Pattern 3 hamburger)
  </read_first>
  <behavior>
    ComparisonView.tsx changes:
    - Adds import { useState } from 'react' at top
    - Adds useStore selectors: lastUpset, logout
    - Adds const [menuOpen, setMenuOpen] = useState(false)
    - Adds handlers: handleSync, handleRefresh, handleLogout (each calls setMenuOpen(false) then the store action)
    - Header becomes: hamburger div (relative, contains button + conditional dropdown) left · counter span center · username span right
    - Dropdown is absolutely positioned, appears when menuOpen, contains Sync/Refresh/Logout items
    - Upset callout rendered after grid, before action buttons, only when lastUpset !== null
    - Callout classes: bg-amber-50 border border-amber-200 text-amber-800 rounded px-4 py-2 text-center text-sm mt-4
    - Callout text: "{lastUpset.winnerName} moved up {lastUpset.spotsGained} {lastUpset.spotsGained === 1 ? 'spot' : 'spots'}"
    - Refresh button REMOVED from action bar (moved to hamburger)
    - Standalone Sync button in old header REPLACED by hamburger (already handled by header rewrite)
    - Skip, Ranked list, Unplayed buttons remain in action bar unchanged

    ComparisonView.test.tsx changes:
    - Update the existing 'ComparisonView Sync to BGG button (SYNC-01, SYNC-02, D-05, D-08)' describe:
      All tests in this block that click or query 'Sync to BGG' must first open the hamburger
      by clicking the button with aria-label "Menu": fireEvent.click(screen.getByRole('button', { name: /menu/i }))
      Then query for the Sync to BGG button inside the now-open dropdown.
    - Do NOT delete existing tests — update them to use the hamburger-open flow.
  </behavior>
  <action>
    Edit ComparisonView.tsx:
    1. Add useState import: change "import { useStore }" line to also include useState from 'react'.
       Full new imports: import { useState } from 'react' (first line), then import { useStore } ... (second line).
    2. Add new selectors after existing ones (after sessionId line):
       const lastUpset = useStore(s => s.lastUpset)
       const logout = useStore(s => s.logout)
    3. Add after the syncDisabled line:
       const [menuOpen, setMenuOpen] = useState(false)
       const handleSync = () => { setMenuOpen(false); startSync() }
       const handleRefresh = () => { setMenuOpen(false); refresh() }
       const handleLogout = () => { setMenuOpen(false); logout() }
    4. Replace the entire <header> element (lines 34-45) with the hamburger pattern from PATTERNS.md.
       The counter span uses U+00B7 (·) as the separator — same as existing text.
       Hamburger button: aria-label="Menu", text ☰
       Dropdown: only shown when menuOpen — absolute positioned div with three buttons.
       Sync item: disabled={syncDisabled} with disabled:opacity-50 disabled:cursor-not-allowed classes.
    5. After the closing </div> of the grid (after line 57), insert the callout row:
       {lastUpset !== null && (<div className="bg-amber-50 border border-amber-200 text-amber-800 rounded px-4 py-2 text-center text-sm mt-4">
         {lastUpset.winnerName} moved up {lastUpset.spotsGained} {lastUpset.spotsGained === 1 ? 'spot' : 'spots'}
       </div>)}
    6. In the action buttons div, remove the Refresh button entirely.
       Keep: Skip, Ranked list, Unplayed buttons.

    Edit ComparisonView.test.tsx:
    1. In the existing describe 'ComparisonView Sync to BGG button ...':
       For every test that calls screen.getByRole('button', { name: /sync to bgg/i }):
       Prepend: fireEvent.click(screen.getByRole('button', { name: /menu/i })) to open hamburger first.
    2. Verify mock object already has startSync (it does) and now also has logout (added in 04-01).

    IMPORTANT: The 'Sync to BGG button is always rendered regardless of disabled state' test:
    This test sets mockDirtyGameIds = [] and checks the button is in the document. After the change,
    the Sync item is inside the hamburger (only visible when open). Update the test to open hamburger first.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/components/ComparisonView.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - ComparisonView.tsx contains "menuOpen" (local state for hamburger)
    - ComparisonView.tsx contains "lastUpset" (store selector and callout render)
    - ComparisonView.tsx contains "bg-amber-50" (callout styling per D-05)
    - ComparisonView.tsx contains "aria-label" and "Menu" (hamburger accessibility)
    - ComparisonView.tsx does NOT contain a standalone Refresh button in the action bar (grep for onClick={refresh} should only appear inside handleRefresh handler definition, not in JSX button)
    - All describe blocks in ComparisonView.test.tsx pass GREEN
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    ComparisonView shows hamburger header, upset callout, and no standalone Refresh/Sync in action bar.
    All ComparisonView.test.tsx tests pass including old Sync tests (updated to use hamburger open flow)
    and new Phase 4 describe blocks from 04-01.
  </done>
</task>

<task type="auto">
  <name>Task 2: Simplify UsernameEntry — remove continue-prompt block and unused selectors</name>
  <files>src/components/UsernameEntry.tsx</files>
  <read_first>
    - src/components/UsernameEntry.tsx (full file — current state)
    - .planning/phases/04-display-polish/04-CONTEXT.md (D-07: auto-resume now in store.login(); prompt not needed)
    - .planning/phases/04-display-polish/04-PATTERNS.md (UsernameEntry section — selectors to remove, JSX to delete)
  </read_first>
  <behavior>
    - Only selector remaining: const login = useStore((s) => s.login)
    - continueSession, resetForNewUser, sessionUsername, rankingsUsername, ratings selectors: REMOVED
    - showContinuePrompt computed variable: REMOVED
    - ratingsCount variable: REMOVED
    - The {showContinuePrompt && (...)} JSX block (lines 101-123): REMOVED entirely
    - handleSubmit unchanged: still calls login(trimmed, trimmedPassword)
    - Form JSX (username field, password field, submit button): unchanged
    - useState hooks for input, validationError, password, passwordError: unchanged
  </behavior>
  <action>
    Edit src/components/UsernameEntry.tsx with targeted deletions:

    1. Remove these useStore selector lines (keep only login):
       - const continueSession = useStore((s) => s.continueSession)
       - const resetForNewUser = useStore((s) => s.resetForNewUser)
       - const sessionUsername = useStore((s) => s.sessionUsername)
       - const rankingsUsername = useStore((s) => s.rankingsUsername)
       - const ratings = useStore((s) => s.ratings)

    2. Remove these computed variable lines:
       - const showContinuePrompt = sessionUsername !== null && sessionUsername === rankingsUsername && Object.keys(ratings).length > 0
       - const ratingsCount = Object.keys(ratings).length

    3. Remove the entire JSX block:
       {showContinuePrompt && (
         <div className="mt-6 pt-6 border-t border-gray-200">
           ... (all lines until and including the closing </div>)
         </div>
       )}

    Do NOT modify handleSubmit, the form, or any useState hooks.
    TypeScript will flag unused variables if removal is incomplete — use the compile check to confirm.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/components/ComparisonView.test.tsx src/store/store.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/UsernameEntry.tsx does NOT contain "showContinuePrompt"
    - src/components/UsernameEntry.tsx does NOT contain "ratingsCount"
    - src/components/UsernameEntry.tsx does NOT contain "continueSession"
    - src/components/UsernameEntry.tsx does NOT contain "resetForNewUser"
    - src/components/UsernameEntry.tsx still contains "login(trimmed, trimmedPassword)" in handleSubmit
    - npx tsc --noEmit exits 0 (no unused variable errors)
    - npm test full suite exits 0
    - Commit: "feat(04-04): ComparisonView hamburger + upset callout; UsernameEntry auto-resume cleanup (DISP-01, DISP-02)"
  </acceptance_criteria>
  <done>
    UsernameEntry.tsx is simplified: login form only, no continue-prompt block.
    Full test suite passes.
    TypeScript compiles clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 complete — all visual features implemented:
    - GameCard shows h-48 cover art (or gray placeholder) with BGG page link
    - Comparison header has hamburger menu (top-left), session counter (center), username (right)
    - Hamburger dropdown contains Sync to BGG, Refresh rankings, Logout
    - Upset callout appears in amber between the card grid and action buttons
    - Login always collects username + password; returning users auto-resume without a prompt
  </what-built>
  <how-to-verify>
    1. Start dev server: npm run dev
    2. Navigate to http://localhost:5173
    3. Log in with a valid BGG username and password
    4. DISP-01 — Cover art:
       - Each game card in the comparison should show a thumbnail image at ~192px height
       - The image should be clickable and open boardgamegeek.com/boardgame/{id} in a new tab
       - If you have games with no thumbnail, verify the gray "No image" placeholder appears
       - Rank should display as "#N" only (no "of N total")
    5. DISP-02 — Upset callout:
       - Play several comparisons
       - When you pick a game that was ranked lower than its opponent, an amber callout
         should appear below the cards: "[Game name] moved up N spots"
       - Wait 5 seconds — callout should disappear automatically
       - If you pick rapidly, confirm the callout doesn't disappear prematurely
    6. D-08/D-09 — Hamburger:
       - Header shows: hamburger icon top-left, counter in center, username on right
       - Click hamburger → dropdown shows Sync to BGG, Refresh rankings, Logout
       - If you have unsaved picks, Sync to BGG should be enabled; otherwise disabled
       - Click Logout → returns to login form; stored rankings preserved (log back in and auto-resume)
    7. D-07 — Auto-resume:
       - Log out, then log back in with the same username
       - Should go directly to comparison screen (no "Found N ranked games" prompt)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ComparisonView → store.logout() | User-initiated action; clears session fields only; rankings preserved per D-08 |
| UsernameEntry form → store.login() | Credentials (username + password) flow through store; no direct bggClient call from component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | sessionUsername rendered in header | accept | Username is non-sensitive display data; already rendered in current header (no change in exposure) |
| T-04-07 | Spoofing | logout() called without auth check | accept | logout() is a client-side session clear; no server-side state to protect; rankings retained for re-login |
| T-04-08 | Tampering | hamburger dropdown outside-click not dismissed | accept | RESEARCH.md recommends skipping outside-click for MVP; menu closes on action click; low risk |
| T-04-09 | Elevation of Privilege | UsernameEntry remove continue-prompt | accept | Prompt removal reduces surface; login() now owns the auth + resume decision; no privilege change |
| T-04-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm test (full suite)
All describe blocks GREEN:
  - 'GameCard thumbnail (DISP-01)'
  - 'Upset callout (DISP-02)'
  - 'Hamburger menu (D-08, D-09)'
  - 'pick() upset detection (D-01, D-02, D-03)'
  - 'login() auto-resume (D-07)'
  - All previously-passing tests

npx tsc --noEmit exits 0

Manual checkpoint: visual verification of all Phase 4 features in browser
</verification>

<success_criteria>
- ComparisonView.tsx has hamburger menu with Sync, Refresh, Logout items
- ComparisonView.tsx renders upset callout with amber styling between grid and action buttons
- ComparisonView.tsx has no standalone Refresh or Sync buttons outside the hamburger
- UsernameEntry.tsx has no showContinuePrompt, ratingsCount, or continue-prompt JSX
- Full npm test suite passes (all describe blocks GREEN)
- TypeScript compiles clean
- Human checkpoint approved
</success_criteria>

<output>
Create .planning/phases/04-display-polish/04-04-SUMMARY.md when done
</output>