---
phase: 04.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/store/store.ts
  - src/store/store.test.ts
autonomous: true
requirements:
  - D-02
  - D-14

must_haves:
  truths:
    - "store.view can be set to 'ranked-grid' via showRankedGrid()"
    - "showRankedGrid() action is present on AppActions interface and implementation"
    - "D-14 test coverage for moveUnplayedToRanked dirty-flag behavior is verified present"
  artifacts:
    - path: "src/store/store.ts"
      provides: "Extended view union + showRankedGrid action"
      contains: "ranked-grid"
    - path: "src/store/store.test.ts"
      provides: "showRankedGrid() unit test + D-14 coverage confirmation"
      contains: "showRankedGrid"
  key_links:
    - from: "src/store/store.ts"
      to: "ComparisonStateSlice.view"
      via: "view union literal type"
      pattern: "ranked-grid"
    - from: "src/store/store.ts"
      to: "AppActions.showRankedGrid"
      via: "interface declaration + set({ view: 'ranked-grid' })"
      pattern: "showRankedGrid"
---

<objective>
Extend the Zustand store with the 'ranked-grid' view state and `showRankedGrid()` action, then add a unit test asserting the state transition. Verify D-14 test coverage is already satisfied by existing tests in `store.test.ts`.

Purpose: Plan 02 (RankedGridView component) and Plan 03 (wiring) both depend on the store having `'ranked-grid'` in its view union and `showRankedGrid()` on its action interface. This Wave 0 plan establishes that contract before parallel work begins.

Output: Updated `store.ts` with 'ranked-grid' union member and `showRankedGrid()` action; new `describe('showRankedGrid() action')` test block in `store.test.ts`; D-14 confirmed closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-list-view-cleanup/04.1-CONTEXT.md
@.planning/phases/04.1-list-view-cleanup/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend store view union and add showRankedGrid() action</name>
  <files>src/store/store.ts</files>
  <read_first>
    - src/store/store.ts — read fully before editing; key locations:
      line 48 (ComparisonStateSlice.view union type to extend)
      lines 61–81 (AppActions interface — add showRankedGrid(): void after showUnplayedList(): void)
      lines 389–391 (showRankedList/showUnplayedList/backToComparison implementations — add showRankedGrid alongside them)
  </read_first>
  <behavior>
    - showRankedGrid() called on a store in 'comparison' view → view becomes 'ranked-grid'
    - showRankedGrid() called on a store in 'ranked-list' view → view becomes 'ranked-grid'
    - backToComparison() called after showRankedGrid() → view becomes 'comparison' (reused, no change needed)
  </behavior>
  <action>
    Make three surgical edits to store.ts:

    1. In ComparisonStateSlice (line 48), extend the view union by appending | 'ranked-grid' after 'unplayed-list':
       view: 'entry' | 'loading' | 'comparison' | 'error' | 'syncing' | 'ranked-list' | 'unplayed-list' | 'ranked-grid'

    2. In AppActions interface (after showUnplayedList(): void, before backToComparison(): void), add:
       showRankedGrid(): void

    3. In the store implementation (alongside showRankedList and showUnplayedList at lines 389–390), add:
       showRankedGrid(): void { set({ view: 'ranked-grid' }) },

    No other changes. Do not alter the persist partialize function — 'ranked-grid' is part of the non-persisted ComparisonStateSlice (view resets to 'comparison' on reload via the existing state initialization).
  </action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts src/store/store.test.ts</automated>
  </verify>
  <done>
    - src/store/store.ts contains the literal string 'ranked-grid' in the view union on ComparisonStateSlice
    - AppActions interface declares showRankedGrid(): void
    - store implementation includes showRankedGrid(): void { set({ view: 'ranked-grid' }) }
    - Full existing test suite (169 tests) passes — no regressions from type change
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add showRankedGrid() unit test + verify D-14 coverage</name>
  <files>src/store/store.test.ts</files>
  <read_first>
    - src/store/store.test.ts lines 983–1010 — read the moveUnplayedToRanked() dirty tracking (SYNC-03) describe block to confirm D-14 is already covered (two tests asserting dirtyGameIds contains the moved game ID)
    - src/store/store.test.ts lines 1–40 — read the import block and createAppStore/setupStoreWithGames helpers to match the existing test pattern
    - src/store/store.test.ts — grep for "showRankedList" to confirm no existing view navigation tests exist (expected: no matches) and identify a nearby describe block to place the new test after
  </read_first>
  <behavior>
    - Test: store starts at view='comparison'; call showRankedGrid(); expect view === 'ranked-grid'
    - Test: store starts at view='ranked-grid'; call backToComparison(); expect view === 'comparison'
  </behavior>
  <action>
    Add a new describe block to store.test.ts for the showRankedGrid() action. Place it immediately after the existing view-navigation actions section or near the end of the file before the last describe block. Use the same createAppStore(createMockStorage()) pattern as other tests in the file.

    New describe block:
      describe('showRankedGrid() action (D-02)', () => {
        it('sets view to ranked-grid from comparison', () => { ... })
        it('backToComparison() returns from ranked-grid to comparison', () => { ... })
      })

    After confirming D-14 tests at lines 983–1010 exist and assert (a) dirtyGameIds.length > 0 after moveUnplayedToRanked and (b) dirtyGameIds.includes('g0'), add a comment in the new describe block's JSDoc or a top-of-block comment: "// D-14: verified covered by moveUnplayedToRanked() dirty tracking (SYNC-03) describe block — no additional test needed"

    Do not duplicate D-14 test logic — comment only confirms the coverage decision.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts src/store/store.test.ts</automated>
  </verify>
  <done>
    - store.test.ts contains a describe('showRankedGrid() action (D-02)') block with at least 2 passing tests
    - test asserts view === 'ranked-grid' after showRankedGrid() call
    - test asserts view === 'comparison' after subsequent backToComparison() call
    - All store tests pass (170+ tests, no regressions)
    - D-14 comment present confirming existing coverage is sufficient
    - Note: D-14 CONTEXT.md says "Phase 4.1 adds a test" — the plan confirms existing tests at lines 983–1010 already cover this assertion per 04.1-RESEARCH.md finding; no duplicate test created. This satisfies D-14's intent without adding redundant coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan makes only internal store type and implementation changes. No new user input, no new API calls, no new data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — zero install surface |

*No new attack surface introduced. View state transitions are pure client-side Zustand state; the 'ranked-grid' value is never sent to a server or persisted to localStorage.*
</threat_model>

<verification>
Run after both tasks complete:

```
npx vitest run --config vitest.config.ts src/store/store.test.ts
```

Expected: All tests pass (170+ total). Specifically:
- describe('showRankedGrid() action (D-02)') — 2 tests green
- describe('moveUnplayedToRanked() dirty tracking (SYNC-03)') — 2 tests still green (regression check)
</verification>

<success_criteria>
- store.ts view union contains 'ranked-grid' literal
- AppActions interface and implementation both declare showRankedGrid(): void
- store.test.ts has showRankedGrid() describe block with passing tests
- D-14 confirmed covered by existing tests; comment documents the decision
- Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/04.1-list-view-cleanup/04.1-01-SUMMARY.md` when done
</output>
