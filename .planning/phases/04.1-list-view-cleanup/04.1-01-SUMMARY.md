---
plan: 04.1-01
status: complete
wave: 0
---

# Plan 04.1-01 Summary: Store Extension — ranked-grid view state

## What Was Built

Extended the Zustand store with the `'ranked-grid'` view state and `showRankedGrid()` action. Added unit tests for the new action. Verified D-14 dirty-flag test coverage is already satisfied.

## Key Files

### Modified
- `src/store/store.ts` — extended view union, added `showRankedGrid(): void` to AppActions interface and implementation
- `src/store/store.test.ts` — added `describe('showRankedGrid() action (D-02)')` with 2 tests; D-14 comment confirming existing coverage

## Decisions Made

- **D-02 implemented:** `showRankedGrid(): void { set({ view: 'ranked-grid' }) }` alongside `showRankedList` at store implementation lines
- **D-14 closed:** Existing `moveUnplayedToRanked() dirty tracking (SYNC-03)` describe block (lines 983–1010 of store.test.ts) fully covers the assertion — no duplicate test added
- **Persist partialize unchanged:** `'ranked-grid'` is in the non-persisted `ComparisonStateSlice`; view resets to `'comparison'` on reload as expected

## Test Results

- **Store tests:** 65 passed (63 pre-existing + 2 new)
- **Full suite:** 171 passed (169 pre-existing + 2 new)
- **Pre-existing unhandled rejections in bggClient.test.ts:** 3 — pre-existing, not caused by this plan

## Self-Check: PASSED

All must_haves satisfied:
- ✓ `store.ts` view union contains `'ranked-grid'`
- ✓ `AppActions` interface declares `showRankedGrid(): void`
- ✓ Store implementation includes `showRankedGrid(): void { set({ view: 'ranked-grid' }) }`
- ✓ `store.test.ts` has `showRankedGrid() action (D-02)` describe block with 2 passing tests
- ✓ D-14 coverage confirmed; comment documents the decision

## Commits

- `feat(04.1-01): extend store view union with 'ranked-grid' and add showRankedGrid() action`
- `test(04.1-01): add showRankedGrid() action tests and verify D-14 coverage`
