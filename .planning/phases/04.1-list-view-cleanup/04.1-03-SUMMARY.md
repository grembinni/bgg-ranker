---
plan: 04.1-03
status: complete
wave: 2
phase: "04.1"
subsystem: ui-wiring
tags: [app-router, navigation, comparison-view, grid-view]
dependency_graph:
  requires:
    - 04.1-01  # store view union with 'ranked-grid' and showRankedGrid()
    - 04.1-02  # RankedGridView component
  provides:
    - ranked-grid navigation wired end-to-end
  affects:
    - src/App.tsx (modified)
    - src/components/ComparisonView.tsx (modified)
tech_stack:
  added: []
  patterns:
    - view router case addition pattern (App.tsx conditional render)
    - useStore selector addition pattern (ComparisonView)
key_files:
  created: []
  modified:
    - src/App.tsx
    - src/components/ComparisonView.tsx
decisions:
  - "D-01: ComparisonView action bar shows '☰ Ranked list' (hamburger prefix) and '⊞ Grid view' buttons side by side"
  - "D-03: RankedListView.tsx byte-for-byte unchanged — no hamburger added to text list view"
metrics:
  duration: "~5 minutes"
  completed: "2026-05-26"
  tasks_completed: 2
  files_created: 0
  files_modified: 2
---

# Phase 4.1 Plan 03 Summary: App.tsx wiring + ComparisonView navigation buttons

## One-liner

Wired RankedGridView into App.tsx's view router and added '☰ Ranked list' prefix and '⊞ Grid view' button to ComparisonView's action bar, closing the ranked-grid navigation loop.

## What Was Built

### Task 1 — App.tsx view router extension

Added two surgical changes to `src/App.tsx`:
- Import: `import RankedGridView from './components/RankedGridView'`
- Render case: `{view === 'ranked-grid' && <RankedGridView />}` after the ranked-list case

No other changes — beforeunload effect, bg-gray-50 wrapper, and all existing view cases are unchanged.

### Task 2 — ComparisonView navigation buttons

Three surgical changes to `src/components/ComparisonView.tsx`:
- Added `const showRankedGrid = useStore(s => s.showRankedGrid)` selector alongside `showRankedList`
- Updated existing "Ranked list" button text to "☰ Ranked list" (Unicode hamburger U+2630 prefix per D-01)
- Added new "⊞ Grid view" button (Unicode grid square U+229E prefix per D-01) with `onClick={showRankedGrid}` and identical className to the Ranked list button

`RankedListView.tsx` confirmed unchanged via `git diff` (D-03 satisfied).

## Key Files

### Modified
- `src/App.tsx` — added RankedGridView import and ranked-grid render case
- `src/components/ComparisonView.tsx` — added showRankedGrid selector, hamburger prefix on Ranked list button, new Grid view button

## Decisions Made

- **D-01 implemented:** ComparisonView action bar now shows three buttons: "Skip", "☰ Ranked list", "⊞ Grid view"
- **D-03 satisfied:** RankedListView.tsx has no modifications — byte-for-byte unchanged

## Test Results

- **Full suite:** 171 passed (171 pre-existing — no new tests needed for JSX wiring)
- **Pre-existing unhandled rejections in bggClient.test.ts:** 3 — pre-existing, not caused by this plan

## Deviations from Plan

None — plan executed exactly as written.

## Threat Surface Scan

No new attack surface. Both changes are pure JSX wiring: an import + render case in App.tsx and a store selector + button in ComparisonView.tsx. The new Grid view button calls `showRankedGrid()` which only sets `view: 'ranked-grid'` in Zustand state — no network calls, no new data flows.

## Known Stubs

None. The ranked-grid navigation is fully wired: ComparisonView -> showRankedGrid() -> view='ranked-grid' -> App.tsx renders RankedGridView -> backToComparison() returns to ComparisonView.

## Self-Check: PASSED

- `src/App.tsx` contains "ranked-grid": CONFIRMED
- `src/App.tsx` imports RankedGridView: CONFIRMED
- `src/components/ComparisonView.tsx` contains "showRankedGrid": CONFIRMED
- `src/components/ComparisonView.tsx` contains "☰ Ranked list": CONFIRMED
- `src/components/ComparisonView.tsx` contains "⊞ Grid view": CONFIRMED
- `src/components/RankedListView.tsx` unchanged (git diff empty): CONFIRMED
- Full test suite passes (171): CONFIRMED

## Commits

- `feat(04.1-03): add ranked-grid case to App.tsx view router` (`1f78397`)
- `feat(04.1-03): update ComparisonView — hamburger prefix on Ranked list + Grid view button` (`3b24443`)
