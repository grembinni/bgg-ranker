---
phase: 4.1
slug: list-view-cleanup
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-26
---

# Phase 4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.7 |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run --config vitest.config.ts src/store/store.test.ts` |
| **Full suite command** | `npx vitest run --config vitest.config.ts` |
| **Estimated runtime** | ~5 seconds (169 tests) |

> **Note:** `npx vitest run` without `--config vitest.config.ts` produces a misleading "0 tests / 6 failed" due to a Vitest v4 runner resolution issue. Always use the explicit config flag.

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run --config vitest.config.ts src/store/store.test.ts`
- **After every plan wave:** Run `npx vitest run --config vitest.config.ts`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| store-extension | 01 | 0 | D-02 | — | N/A | unit | `npx vitest run --config vitest.config.ts src/store/store.test.ts` | ❌ W0 | ⬜ pending |
| d14-verify | 01 | 0 | D-14 | — | N/A | unit | `npx vitest run --config vitest.config.ts src/store/store.test.ts` | ✅ lines 983–1010 | ⬜ pending |
| ranked-grid-view | 02 | 1 | D-05/D-06/D-07/D-08/D-09/D-11/D-12/D-13 | — | N/A | e2e/manual | `npx vitest run --config vitest.config.ts` | ❌ new component | ⬜ pending |
| comparison-view-edit | 03 | 2 | D-01/D-03/D-04 | — | N/A | manual | `npx vitest run --config vitest.config.ts` | ✅ ComparisonView.tsx | ⬜ pending |
| app-router | 03 | 2 | D-02 | — | N/A | manual | `npx vitest run --config vitest.config.ts` | ✅ App.tsx | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/store/store.test.ts` — add `describe('showRankedGrid() action')` block asserting `view` transitions to `'ranked-grid'` (D-02)
- [ ] `src/store/store.test.ts` — verify existing `moveUnplayedToRanked dirty tracking (SYNC-03)` describe block (lines 983–1010) is sufficient for D-14; mark closed if so

*RankedGridView is a UI-only component — no dedicated unit test; manual verification covers DnD and image preloading behaviors.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Grid DnD reorder updates list and marks dirty | D-08/D-09/D-10 | @dnd-kit pointer events not testable in JSDOM | Drag a thumbnail to a new position; verify order changes and sync button appears |
| Image preloading hides re-fetches on page nav | D-12 | Requires real network/browser cache | Navigate Next 50 then Prev 50; open DevTools Network — no new image requests for already-loaded thumbnails |
| Gray placeholder for missing thumbnails | D-06/D-13 | Requires real BGG game data | View a game with no thumbnail; verify gray bg-gray-100 placeholder renders |
| Pagination range display | D-07 | Visual verification | Confirm header shows "Rankings 1–100 of N" format; advance pages and verify range updates |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
