---
phase: 04.1.1-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.tsx
  - src/components/Header.test.tsx
  - src/App.tsx
autonomous: true
requirements: []          # Inserted phase — no REQ-IDs. Traceability via D-01..D-05 (CONTEXT.md).
must_haves:
  truths:
    - "A single Header component renders on all four game views (comparison, ranked-list, ranked-grid, unplayed-list) via App.tsx"
    - "Header shows, left to right: hamburger menu, session/total comparison counts, vs/list/grid view-switch icons, username"
    - "Hamburger menu contains Sync to BGG (N), Refresh rankings, Unplayed (N), Logout"
    - "The view-switch icon matching the current view is highlighted"
    - "Clicking the vs icon calls backToComparison(); list calls showRankedList(); grid calls showRankedGrid(); Unplayed menu item calls showUnplayedList()"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Universal header component (default export)"
      contains: "export default function Header"
      min_lines: 60
    - path: "src/components/Header.test.tsx"
      provides: "Header behavior tests (hamburger, counts, icons, username)"
      contains: "describe("
    - path: "src/App.tsx"
      provides: "Conditional Header render for the four game views"
      contains: "<Header />"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/Header.tsx"
      via: "import Header + conditional render for game views"
      pattern: "import Header"
    - from: "src/components/Header.tsx"
      to: "store view-switch actions"
      via: "onClick handlers"
      pattern: "backToComparison|showRankedList|showRankedGrid|showUnplayedList"
---

<objective>
Create the universal `Header` component (D-01, D-02, D-03) by extracting ComparisonView's existing header/hamburger block, adding an "Unplayed" menu item and a vs/list/grid view-switch icon cluster with active-view highlighting, then wire it into `App.tsx` so it renders on all four game views (D-04). `backToComparison()` becomes the vs icon's action (D-05).

Purpose: One shared header replaces four hand-rolled per-view headers, giving consistent navigation across the app.
Output: `src/components/Header.tsx`, `src/components/Header.test.tsx`, and `App.tsx` wiring. No per-view header removal happens here (that is Plan 02) — after this plan the comparison view transiently shows both its old header and the new one; harmless and resolved in Wave 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1.1-ui-cleanup/04.1.1-CONTEXT.md
@.planning/phases/04.1.1-ui-cleanup/04.1.1-UI-SPEC.md
@.planning/phases/04.1.1-ui-cleanup/04.1.1-PATTERNS.md
@src/components/ComparisonView.tsx
@src/components/ComparisonView.test.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Header.tsx universal header component with tests</name>
  <files>src/components/Header.tsx, src/components/Header.test.tsx</files>
  <read_first>
    - src/components/ComparisonView.tsx (lines 1-2 imports, 7-33 selectors + hamburger state/timer, 47-86 header markup — the exact block being extracted)
    - src/components/ComparisonView.test.tsx (existing hamburger/menu tests — the pattern to migrate and extend; the mock-store shape at lines 21-48 is the template for Header.test.tsx)
    - .planning/phases/04.1.1-ui-cleanup/04.1.1-UI-SPEC.md (Layout Contract + Copywriting Contract — exact glyphs, aria-labels, class strings)
    - .planning/phases/04.1.1-ui-cleanup/04.1.1-PATTERNS.md (Header.tsx pattern assignment — verbatim extraction guidance)
  </read_first>
  <behavior>
    - Test: hamburger button (aria-label "Menu") is present; dropdown items are hidden until clicked.
    - Test: clicking hamburger reveals four items — "Sync to BGG ({N})", "Refresh rankings", "Unplayed ({N})", "Logout".
    - Test: "Sync to BGG" item is disabled when dirtyGameIds is empty OR sessionId is null; enabled otherwise.
    - Test: clicking Sync calls startSync(); Refresh calls refresh(); Unplayed calls showUnplayedList(); Logout calls logout().
    - Test: session-count text renders "{sessionComparisons} this session · {comparisonsTotal} total" and username renders {sessionUsername}.
    - Test: three view-switch buttons exist with aria-labels "Comparison view" (⚔), "Ranked list view" (☰), "Grid view" (⊞); clicking them calls backToComparison(), showRankedList(), showRankedGrid() respectively.
    - Test: when view === 'ranked-grid', the grid button has active classes (text-blue-600 bg-blue-50); the others do not.
  </behavior>
  <action>
    Create `src/components/Header.tsx` as a default-exported `Header` function component. Import only `useState`, `useRef` from react and `useStore` from '../store/store' — do NOT import GameCard. Use one `useStore(s => s.field)` call per field (project convention), selecting: sessionComparisons, comparisonsTotal, sessionUsername, dirtyGameIds, unplayedIds, refresh, startSync, showRankedList, showRankedGrid, showUnplayedList, backToComparison, sessionId, logout, and view (view is NEW vs. the original ComparisonView block — needed for active-icon highlight). Compute `syncCount = dirtyGameIds.length` and `syncDisabled = !sessionId || syncCount === 0`.

    Reuse the hamburger state/timer pattern verbatim from ComparisonView lines 27-33: `menuOpen` state, `menuCloseTimer` ref, `handleMenuEnter`/`handleMenuLeave` (150ms close delay), and `handleSync`/`handleRefresh`/`handleLogout` (each does setMenuOpen(false) then the action). Add a NEW `handleUnplayed` that does setMenuOpen(false) then showUnplayedList().

    Render a wrapper `<div className="max-w-6xl mx-auto px-4 pt-8">` containing the header element `<header className="flex justify-between items-center mb-8 text-base text-gray-700">`. This max-w-6xl wrapper is the planner's resolution of the UI-SPEC container discretion item: it makes the header byte-identical across all four views (SC #1) and preserves the px-4 horizontal-padding convention. Inside the header, in this left-to-right order (D-02):
    1. Hamburger dropdown block (relative div with onMouseEnter/onMouseLeave). Toggle button className "px-3 py-1.5 border border-gray-200 rounded text-sm text-gray-700 hover:bg-gray-50 active:bg-gray-100 outline-2 outline-offset-2 outline-blue-600", aria-label "Menu", glyph ☰. Dropdown container className "absolute left-0 top-full mt-1 w-44 bg-white border border-gray-200 rounded shadow-sm z-20 flex flex-col". Menu items in order: Sync ("Sync to BGG ({syncCount})", disabled={syncDisabled}, className "px-4 py-2 text-sm text-left text-gray-700 hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed"), Refresh ("Refresh rankings"), Unplayed ("Unplayed ({unplayedIds.length})") — NEW item per D-02, Logout ("Logout") — the last three share className "px-4 py-2 text-sm text-left text-gray-700 hover:bg-gray-50".
    2. `<span>` with "{sessionComparisons} this session · {comparisonsTotal} total".
    3. View-switch cluster `<div className="flex gap-2">` with three buttons, each base className "px-3 py-1.5 rounded text-sm" plus active/inactive variant: active (matching view) appends "text-blue-600 bg-blue-50", inactive appends "text-gray-700 hover:bg-gray-50". Buttons: ⚔ onClick backToComparison aria-label "Comparison view" active-when view==='comparison'; ☰ onClick showRankedList aria-label "Ranked list view" active-when view==='ranked-list'; ⊞ onClick showRankedGrid aria-label "Grid view" active-when view==='ranked-grid'.
    4. `<span>{sessionUsername}</span>`.

    Create `src/components/Header.test.tsx` following ComparisonView.test.tsx's structure: `// @vitest-environment jsdom` header, vi.mock of '../store/store' returning a selector over a mutable mock-state object (mirror the shape at ComparisonView.test.tsx lines 21-48 but add `view` and the four view-switch action mocks: backToComparison, showRankedGrid, and reuse showRankedList/showUnplayedList). Cover every case in the behavior block above. Do NOT re-test GameCard or upset-callout here (those stay in ComparisonView.test.tsx).
  </action>
  <verify>
    <automated>npx vitest run src/components/Header.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Header.tsx` exists and contains "export default function Header".
    - Header.tsx contains the glyphs ⚔, ☰, ⊞ and aria-labels "Comparison view", "Ranked list view", "Grid view".
    - Header.tsx contains a menu item with text "Unplayed (" bound to showUnplayedList.
    - Header.tsx active-icon logic references `view === 'comparison'`, `view === 'ranked-list'`, `view === 'ranked-grid'` and applies "text-blue-600 bg-blue-50" on match.
    - `npx vitest run src/components/Header.test.tsx` exits 0 with all behavior-block cases passing.
    - `npx tsc -b` reports no type errors introduced by Header.tsx.
  </acceptance_criteria>
  <done>Header component renders hamburger (Sync/Refresh/Unplayed/Logout), counts, three view-switch icons with active highlight, and username; all Header.test.tsx cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Header into App.tsx for the four game views</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current flat conditional render, lines 27-38 — no shared layout wrapper today)
    - src/components/Header.tsx (the component created in Task 1 — confirm default export name)
  </read_first>
  <action>
    In `src/App.tsx`, add `import Header from './components/Header'` alongside the existing component imports. Inside the returned `<div className="bg-gray-50 min-h-screen">`, compute a boolean `showHeader = view === 'comparison' || view === 'ranked-list' || view === 'ranked-grid' || view === 'unplayed-list'` and render `{showHeader && <Header />}` as the FIRST child inside that div, before the existing per-view conditionals. Do NOT render Header for 'entry', 'loading', 'error', or 'syncing' views (they have no header and are out of scope). Leave all existing `{view === '...' && <Component />}` lines unchanged.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `import Header from './components/Header'`.
    - src/App.tsx contains `{showHeader && <Header />}` and a `showHeader` expression covering exactly the four values 'comparison', 'ranked-list', 'ranked-grid', 'unplayed-list'.
    - `npx tsc -b` passes with no errors.
    - Full `npx vitest run` suite exits 0 (no existing test regressed by the App.tsx change).
  </acceptance_criteria>
  <done>App.tsx renders the universal Header above the four game views only; type-check and full test suite pass.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols/files created by this plan — exclude from source-drift verification:
- `src/components/Header.tsx` — new file; default export `Header` React component.
- `Header` component internal handler `handleUnplayed` (new; not present in the original ComparisonView block).
- New glyph usage: ⚔ (comparison/"vs" view-switch icon, aria-label "Comparison view").
- `src/components/Header.test.tsx` — new test file.
- `src/App.tsx` new local const `showHeader` (boolean) and `<Header />` render site.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (DOM events) → Zustand store | Header click handlers invoke existing store actions; no external input crosses a network or persistence boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.1-01 | Tampering | Header view-switch / menu actions | accept | Client-only SPA state transitions (view, menu open/close); no privilege change, no server trust. Actions are the same pre-existing store actions already shipped in Phase 4.1 |
| T-04.1.1-02 | Information Disclosure | `{sessionUsername}` rendered in header | accept | Username is already user-visible and non-secret; password/sessionId are never rendered. No new disclosure surface |
| T-04.1.1-03 | Elevation of Privilege | Sync/Logout menu items | mitigate | Sync stays disabled unless `sessionId` present and `dirtyGameIds` non-empty (existing guard reused verbatim); no new bypass introduced |
| T-04.1.1-SC | Tampering | package installs | accept | No new dependencies installed in this phase (verified against package.json); package legitimacy gate not applicable |
</threat_model>

<verification>
- `npx vitest run src/components/Header.test.tsx` passes.
- `npx vitest run` full suite passes (no regressions).
- `npx tsc -b` passes.
- Header renders hamburger + counts + ⚔/☰/⊞ icons + username in App.tsx for the four game views.
</verification>

<success_criteria>
- Header.tsx exists as a default-exported component covering D-02 contents in D-02 order.
- vs/list/grid icons use glyphs ⚔/☰/⊞ (D-03) and call the correct store actions (D-05).
- Active view icon is highlighted.
- App.tsx renders Header for exactly the four game views (D-01, D-04).
- All tests and type-check pass.
</success_criteria>

<output>
Create `.planning/phases/04.1.1-ui-cleanup/04.1.1-01-SUMMARY.md` when done.
</output>
