---
phase: 04.1.1-ui-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1.1-01
files_modified:
  - src/components/ComparisonView.tsx
  - src/components/RankedListView.tsx
  - src/components/RankedGridView.tsx
  - src/components/UnplayedListView.tsx
  - src/components/ComparisonView.test.tsx
autonomous: true
requirements: []          # Inserted phase — no REQ-IDs. Traceability via D-04..D-09 (CONTEXT.md).
must_haves:
  truths:
    - "No per-view <header> block remains in ComparisonView, RankedListView, RankedGridView, or UnplayedListView"
    - "ComparisonView no longer renders the Ranked list / Grid view / Unplayed nav buttons"
    - "ComparisonView's Skip button sits to the right of the two GameCards, styled red, full column height"
    - "The lastUpset callout keeps its current position (between the cards row and where the old button row was)"
    - "RankedListView, RankedGridView, and UnplayedListView no longer render a back button"
  artifacts:
    - path: "src/components/ComparisonView.tsx"
      provides: "Cleaned comparison view — no header, no nav row, Skip as 3rd grid column"
      contains: "grid-cols-[1fr_1fr_auto]"
    - path: "src/components/ComparisonView.test.tsx"
      provides: "Updated tests — Skip styling + nav-buttons-absent; hamburger tests removed (migrated to Header.test.tsx)"
      contains: "describe("
  key_links:
    - from: "src/components/ComparisonView.tsx"
      to: "skip store action"
      via: "Skip button onClick"
      pattern: "onClick=\\{skip\\}"
---

<objective>
Remove every per-view `<header>` block now that Plan 01's universal Header renders from App.tsx (D-04, D-05), and clean up ComparisonView: delete the Ranked list / Grid view / Unplayed nav button row (D-06), relocate the Skip button to the right of the two GameCards as a full-height red column (D-07, D-08), and keep the lastUpset callout in place (D-09). Update ComparisonView.test.tsx to drop the hamburger/menu tests (migrated to Header.test.tsx in Plan 01) and assert the new Skip styling and the absence of nav buttons.

Purpose: Finish the switch-over to the universal header and give ComparisonView its final cleaned layout.
Output: Four modified view components with headers removed, ComparisonView's Skip relocated/restyled, and updated ComparisonView tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1.1-ui-cleanup/04.1.1-CONTEXT.md
@.planning/phases/04.1.1-ui-cleanup/04.1.1-UI-SPEC.md
@.planning/phases/04.1.1-ui-cleanup/04.1.1-PATTERNS.md
@src/components/ComparisonView.tsx
@src/components/ComparisonView.test.tsx
@src/components/RankedListView.tsx
@src/components/RankedGridView.tsx
@src/components/UnplayedListView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up ComparisonView — remove header, remove nav row, relocate Skip</name>
  <files>src/components/ComparisonView.tsx</files>
  <read_first>
    - src/components/ComparisonView.tsx (full file — current header block lines 47-86, cards grid line 87, upset callout lines 99-103, button row lines 104-133)
    - .planning/phases/04.1.1-ui-cleanup/04.1.1-UI-SPEC.md (Layout Contract — 3-column grid + Skip button class string)
    - .planning/phases/04.1.1-ui-cleanup/04.1.1-PATTERNS.md (ComparisonView modify-in-place section — exact removals)
  </read_first>
  <action>
    Edit `src/components/ComparisonView.tsx`:
    1. Remove the entire `<header>` block (current lines 47-86). It now lives in the universal Header component rendered by App.tsx.
    2. Remove the imports and selectors that only served the removed header/nav row: drop `useState` and `useRef` from the react import; drop the store selectors sessionComparisons, comparisonsTotal, sessionUsername, dirtyGameIds, unplayedIds, refresh, startSync, showRankedList, showRankedGrid, showUnplayedList, sessionId, logout; and remove the derived syncCount/syncDisabled and the menuOpen/menuCloseTimer/handleMenuEnter/handleMenuLeave/handleSync/handleRefresh/handleLogout block (lines 24-33). KEEP selectors: currentPair, pick, skip, markUnplayed, lastUpset. Keep the GameCard import and the `currentPair === null` early-return (lines 35-41) unchanged.
    3. Remove the entire button row (current lines 104-133) — the Skip, "☰ Ranked list", "⊞ Grid view", and "Unplayed (N)" buttons.
    4. Change the cards container from `grid grid-cols-2 gap-6` (line 87) to `grid grid-cols-[1fr_1fr_auto] items-stretch gap-6` and add a THIRD column: a Skip `<button type="button" onClick={skip}>` with className "h-full px-6 bg-red-500 hover:bg-red-600 active:bg-red-700 text-white text-sm font-medium rounded outline-2 outline-offset-2 outline-red-600 flex items-center justify-center" and label "Skip". The `h-full` on an `items-stretch` grid row makes Skip match the tallest GameCard height with no hardcoded pixel value (D-08); px-6 keeps the same width as today (D-08).
    5. Leave the lastUpset callout block (current lines 99-103) exactly as-is and in its current position — after the cards grid (D-09). After this edit, nothing renders below the callout (the old button row is gone).
  </action>
  <verify>
    <automated>npx vitest run src/components/ComparisonView.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - ComparisonView.tsx no longer contains the string `<header` nor the labels "Ranked list", "Grid view", or "☰".
    - ComparisonView.tsx contains `grid grid-cols-[1fr_1fr_auto] items-stretch gap-6`.
    - ComparisonView.tsx Skip button contains `bg-red-500`, `h-full`, and `outline-red-600` and `onClick={skip}`.
    - ComparisonView.tsx no longer imports `useState`/`useRef` and no longer references `startSync`, `showRankedList`, `showRankedGrid`, `showUnplayedList`, or `logout`.
    - The lastUpset callout block (`bg-amber-50 ... moved up`) is still present, unchanged.
    - `npx tsc -b` passes (no unused-variable or type errors).
  </acceptance_criteria>
  <done>ComparisonView shows two GameCards plus a full-height red Skip column, keeps the upset callout, and has no header or nav buttons; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove header/back-button blocks from the three list/grid views</name>
  <files>src/components/RankedListView.tsx, src/components/RankedGridView.tsx, src/components/UnplayedListView.tsx</files>
  <read_first>
    - src/components/RankedListView.tsx (header block lines 118-129; backToComparison selector line 89)
    - src/components/RankedGridView.tsx (header block lines 120-129; backToComparison selector line 62; pagination lines 131-151 and side click zones lines 154-169 — MUST stay)
    - src/components/UnplayedListView.tsx (header block lines 71-82; backToComparison selector line 58)
    - .planning/phases/04.1.1-ui-cleanup/04.1.1-PATTERNS.md (per-view modify-in-place sections)
  </read_first>
  <action>
    In each of the three files, remove the standalone `<header className="flex items-center justify-between mb-...">` block containing the `<h1>` title and the "← Back" button (RankedListView lines 120-129, RankedGridView lines 120-129, UnplayedListView lines 73-82). The universal Header (rendered by App.tsx from Plan 01) replaces these. Also remove the now-unused `const backToComparison = useStore(s => s.backToComparison)` selector from each file (RankedListView line 89, RankedGridView line 62, UnplayedListView line 58) — confirm no other usage remains in each file before deleting. Keep everything else unchanged: RankedListView's DndContext/SortableContext list (lines 130-152); RankedGridView's pagination controls (lines 131-151), side click zones (lines 154-169), and DndContext grid (lines 171-184); UnplayedListView's empty-state and row rendering (lines 83-101). Do NOT alter the outer container divs (`max-w-2xl mx-auto px-4 py-8` / `max-w-6xl mx-auto px-4 py-8`) — the content keeps its own width; only the header block is removed.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - None of RankedListView.tsx, RankedGridView.tsx, UnplayedListView.tsx contains the string `← Back` or a `<header` element.
    - None of the three files still references `backToComparison`.
    - RankedGridView.tsx still contains its pagination controls (`Prev 100`/`Next 100`) and the `grid grid-cols-10 gap-1` grid.
    - UnplayedListView.tsx still contains "No games marked as unplayed yet." and the rank `<input>`.
    - `npx tsc -b` passes with no unused-variable errors.
    - Full `npx vitest run` suite exits 0.
  </acceptance_criteria>
  <done>All three list/grid views lose their title+back-button header and the unused backToComparison selector; their content and behavior are otherwise unchanged; type-check and tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update ComparisonView.test.tsx — drop migrated hamburger tests, add Skip + nav-absence tests</name>
  <files>src/components/ComparisonView.test.tsx</files>
  <read_first>
    - src/components/ComparisonView.test.tsx (full file — describe blocks "ComparisonView Sync to BGG button" lines 60-106 and "Hamburger menu" lines 158-205 test the header/menu that no longer lives in ComparisonView)
    - src/components/ComparisonView.tsx (post-Task-1 state — the Skip button and remaining structure the tests must match)
    - src/components/Header.test.tsx (Plan 01 — confirm the hamburger/menu/icon behavior is already covered there so it is safe to delete here)
  </read_first>
  <action>
    Edit `src/components/ComparisonView.test.tsx`:
    1. DELETE the two describe blocks that test the (now-removed) header/menu: "ComparisonView Sync to BGG button (...)" (lines 60-106) and "Hamburger menu (...)" (lines 158-205). Those behaviors are now covered by Header.test.tsx.
    2. Remove the mock entries and unused mock fns that only served the deleted tests (mockStartSync, mockRefresh, mockShowRankedList, mockShowUnplayedList, mockLogout, and the sessionComparisons/comparisonsTotal/sessionUsername/dirtyGameIds/unplayedIds/sessionId/startSync/refresh/showRankedList/showUnplayedList/logout entries in the vi.mock selector object) IF no retained test uses them. Keep mockPick, mockSkip, mockMarkUnplayed, mockLastUpset, mockCurrentPair, and the games/ratings mock — the GameCard and Upset-callout describe blocks still need them.
    3. KEEP the "GameCard thumbnail (DISP-01)" and "Upset callout (DISP-02)" describe blocks unchanged.
    4. ADD a new describe block "Skip button + nav cleanup (D-06, D-07, D-08)" with tests: (a) a button with accessible name "Skip" exists and clicking it calls mockSkip once; (b) the Skip button's className contains "bg-red-500" and "h-full"; (c) there is no button with accessible name matching /ranked list/i, /grid view/i, or /^unplayed/i in ComparisonView (nav moved to the header).
  </action>
  <verify>
    <automated>npx vitest run src/components/ComparisonView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ComparisonView.test.tsx no longer contains describe blocks referencing "Sync to BGG button" or "Hamburger menu".
    - ComparisonView.test.tsx contains a describe block asserting Skip calls skip, Skip has bg-red-500 + h-full, and no Ranked list/Grid view/Unplayed buttons render.
    - `npx vitest run src/components/ComparisonView.test.tsx` exits 0.
    - No unused-variable lint/type error from leftover mock declarations (`npx tsc -b` passes).
  </acceptance_criteria>
  <done>ComparisonView.test.tsx tests only the concerns ComparisonView still owns (GameCard, upset callout, Skip button, nav-absence); hamburger coverage lives in Header.test.tsx; the file passes.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

This plan is primarily deletion + relocation; new symbols introduced:
- `src/components/ComparisonView.tsx`: new Skip `<button>` as a third grid column (relocated + restyled; `grid-cols-[1fr_1fr_auto]` layout).
- `src/components/ComparisonView.test.tsx`: new describe block "Skip button + nav cleanup (D-06, D-07, D-08)".
- Removed symbols (no longer present): per-view `<header>` blocks and back buttons in all four views; ComparisonView's hamburger state/handlers and nav-button row; `backToComparison` selectors in the three list/grid views.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (DOM events) → Zustand store | Skip button and remaining view controls invoke existing store actions; no new network or persistence boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.1-04 | Tampering | Relocated Skip button | accept | Skip triggers the existing `skip()` store action only (pair rotation); no data mutation, no privilege change. Red styling is cosmetic (D-08), not a destructive-confirmation trigger — documented in UI-SPEC |
| T-04.1.1-05 | Denial of Service | Removal of per-view back buttons | accept | Navigation still fully available via the universal Header (vs/list/grid icons + hamburger). No dead-end state: every view reachable and exitable through the header |
| T-04.1.1-SC | Tampering | package installs | accept | No new dependencies installed in this phase (verified against package.json); package legitimacy gate not applicable |
</threat_model>

<verification>
- `npx vitest run` full suite passes.
- `npx tsc -b` passes.
- ComparisonView has no header/nav buttons; Skip is a full-height red column right of the cards; upset callout preserved.
- The three list/grid views have no header/back button but retain all list/grid/pagination behavior.
</verification>

<success_criteria>
- No `<header>` block remains in any of the four game views (D-04, D-05).
- ComparisonView nav buttons removed (D-06); Skip relocated (D-07) and red full-height (D-08); upset callout unchanged (D-09).
- ComparisonView.test.tsx updated; full suite and type-check green.
</success_criteria>

<output>
Create `.planning/phases/04.1.1-ui-cleanup/04.1.1-02-SUMMARY.md` when done.
</output>
