---
phase: 04.1.1-ui-cleanup
plan: 02
subsystem: ui
tags: [react, tailwind, zustand, cleanup]

# Dependency graph
requires:
  - phase: 04.1.1-ui-cleanup (plan 01)
    provides: Universal Header component (src/components/Header.tsx) rendered from App.tsx above all four game views
provides:
  - ComparisonView with no per-view header/nav — two GameCards plus a full-height red Skip column, upset callout unchanged
  - RankedListView, RankedGridView, UnplayedListView with no per-view header/back-button (navigation lives solely in the universal Header)
affects: [04.1.1 remaining plans, any future ComparisonView/RankedListView/RankedGridView/UnplayedListView work]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "3-column stretch grid for Skip relocation: `grid grid-cols-[1fr_1fr_auto] items-stretch gap-6` + Skip's `h-full` matches tallest GameCard with no hardcoded pixel height"
    - "Per-view header removal pattern: delete the `<header>` block and its now-unused `backToComparison`/hamburger-only selectors; outer container div (`max-w-2xl`/`max-w-6xl mx-auto px-4 py-8`) stays untouched since the universal Header supplies its own wrapper"

key-files:
  created: []
  modified:
    - src/components/ComparisonView.tsx
    - src/components/ComparisonView.test.tsx
    - src/components/RankedListView.tsx
    - src/components/RankedGridView.tsx
    - src/components/UnplayedListView.tsx

key-decisions:
  - "ComparisonView.tsx dropped 14 now-unused store selectors and the useState/useRef hamburger-timer block entirely — those concerns moved wholesale to Header.tsx in Plan 01, so ComparisonView now only selects currentPair, pick, skip, markUnplayed, lastUpset"
  - "Skip button retained its existing label/onClick contract (skip()) — only styling and position changed (red trio bg-red-500/hover:bg-red-600/active:bg-red-700, h-full for grid-stretch height matching, moved from a centered button row to the third grid column)"
  - "Test coverage for hamburger/sync/logout/menu behavior was NOT re-added to ComparisonView.test.tsx — confirmed already present in Header.test.tsx (20 tests, Plan 01) before deleting the two obsolete describe blocks"

patterns-established:
  - "View files import only the store selectors they still use post-header-removal — no leftover selector hooks kept 'just in case'"

requirements-completed: []

# Metrics
duration: ~10min
completed: 2026-07-17
---

# Phase 04.1.1 Plan 02: Header/Nav Cleanup + Skip Relocation Summary

**Removed the now-redundant per-view `<header>` blocks from all four game views and gave ComparisonView its final layout — two GameCards plus a full-height red Skip column, upset callout preserved, no nav-button row.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-07-17T10:44:00-05:00
- **Completed:** 2026-07-17T10:47:08-05:00
- **Tasks:** 3 completed
- **Files modified:** 5

## Accomplishments
- `ComparisonView.tsx`: removed the entire hamburger `<header>` block (menu state/timer/handlers, session counts, username) and the Ranked-list/Grid-view/Unplayed nav-button row; cards container changed from `grid grid-cols-2 gap-6` to `grid grid-cols-[1fr_1fr_auto] items-stretch gap-6` with Skip as a full-height red third column; lastUpset callout kept in its original position, unchanged
- `RankedListView.tsx`, `RankedGridView.tsx`, `UnplayedListView.tsx`: removed the title+"← Back" `<header>` block and the now-unused `backToComparison` selector from each; pagination, side click zones, DnD grid/list, and empty-state rendering all unchanged
- `ComparisonView.test.tsx`: deleted the "Sync to BGG button" and "Hamburger menu" describe blocks (11 tests) whose coverage now lives in `Header.test.tsx`; added a new "Skip button + nav cleanup (D-06, D-07, D-08)" describe block (3 tests: Skip calls `skip()`, Skip has `bg-red-500`/`h-full` styling, no Ranked-list/Grid-view/Unplayed buttons render); kept the GameCard thumbnail and Upset callout describe blocks unchanged

## Task Commits

Each task was committed atomically:

1. **Task 1: Clean up ComparisonView — remove header, remove nav row, relocate Skip** — `3b60b59` (feat)
2. **Task 2: Remove header/back-button blocks from the three list/grid views** — `31bd0a7` (feat)
3. **Task 3: Update ComparisonView.test.tsx — drop migrated hamburger tests, add Skip + nav-absence tests** — `79a8882` (test)

_Plan metadata commit created separately after this summary._

## Files Created/Modified
- `src/components/ComparisonView.tsx` — header/nav removed; Skip relocated as red full-height 3rd grid column; upset callout preserved
- `src/components/ComparisonView.test.tsx` — hamburger/sync tests removed (migrated to Header.test.tsx); Skip + nav-absence tests added
- `src/components/RankedListView.tsx` — title+back-button header removed; `backToComparison` selector dropped
- `src/components/RankedGridView.tsx` — title+back-button header removed; `backToComparison` selector dropped; pagination/side-click-zones/grid unchanged
- `src/components/UnplayedListView.tsx` — title+back-button header removed; `backToComparison` selector dropped

## Decisions Made
- Confirmed via reading `Header.test.tsx` (before deleting anything) that all 11 removed ComparisonView test cases (Sync to BGG enable/disable logic, hamburger visibility, menu-item click handlers, Logout/Refresh) have equivalent coverage already in Plan 01's `Header.test.tsx` — no coverage gap introduced.
- Left the outer container divs (`max-w-2xl mx-auto px-4 py-8` / `max-w-6xl mx-auto px-4 py-8`) untouched in all three list/grid views per plan instruction — the universal Header supplies its own `max-w-6xl mx-auto px-4 pt-8` wrapper (Plan 01), so no container-width conflict.

## Deviations from Plan

None - plan executed exactly as written. All three tasks matched their specified removals/additions exactly; no additional sites were found during implementation.

## Issues Encountered
- Task 1's `<verify>` command (`npx vitest run src/components/ComparisonView.test.tsx && npx tsc -b`) intentionally failed after Task 1 alone (11 hamburger/sync tests still referenced the just-removed header markup) — this is the expected, plan-designed transient state between Task 1 and Task 3, which updates the test file to match. Full `npx vitest run src/components/ComparisonView.test.tsx` passes (10/10) after Task 3.
- `npx tsc -b` reports the same 28 pre-existing type errors in `src/store/store.test.ts`/`src/store/store.ts` documented in Plan 01's and Plan 03's summaries (unrelated `AppStore`/`RawGame` typing gaps, one unused `redistribute` import) — verified unchanged before and after this plan's three commits. None of this plan's five modified files appear in the error list.
- Full `npx vitest run` after all three tasks: 187/187 tests passed, 7/7 files passed. Same 3 pre-existing "Unhandled Rejection" console log lines from `src/api/bggClient.test.ts` (expected-error-path tests) noted in Plan 01/03 summaries — file not touched by this plan.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- All four game views (`ComparisonView`, `RankedListView`, `RankedGridView`, `UnplayedListView`) now render exclusively through the universal `Header` from App.tsx — no duplicate/orphaned header markup remains anywhere in the app.
- ComparisonView's final layout (`grid-cols-[1fr_1fr_auto]` with red full-height Skip column) is in place and ready for any remaining Plan 04 work in this phase.
- `backToComparison()` remains defined in the store and wired to the header's ⚔ icon (Plan 01) — only the per-view duplicate call sites were removed here.

---
*Phase: 04.1.1-ui-cleanup*
*Completed: 2026-07-17*

## Self-Check: PASSED

- FOUND: src/components/ComparisonView.tsx
- FOUND: src/components/ComparisonView.test.tsx
- FOUND: src/components/RankedListView.tsx
- FOUND: src/components/RankedGridView.tsx
- FOUND: src/components/UnplayedListView.tsx
- FOUND commit: 3b60b59 (feat, Task 1)
- FOUND commit: 31bd0a7 (feat, Task 2)
- FOUND commit: 79a8882 (test, Task 3)
