---
phase: 04.1.1-ui-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/store.ts
  - src/store/store.test.ts
autonomous: true
requirements: []          # Inserted phase — no REQ-IDs. Traceability via D-10, D-11 (CONTEXT.md).
must_haves:
  truths:
    - "After login/collection load, the app lands on the ranked-grid view (not comparison)"
    - "After continueSession (auto-resume), the app lands on ranked-grid"
    - "After a completed sync, the app returns to ranked-grid"
    - "After reAuthAndResume with no dirty games, the view is ranked-grid"
    - "After cancelSync / session-expiry 401 reset, the view is ranked-grid"
    - "backToComparison() still sets view to comparison (unchanged, wired to the header vs icon)"
  artifacts:
    - path: "src/store/store.ts"
      provides: "Default landing/reset view changed to ranked-grid at the five automatic transition sites"
      contains: "view: 'ranked-grid'"
    - path: "src/store/store.test.ts"
      provides: "Assertions updated to ranked-grid + explicit per-site default-view tests"
      contains: "toBe('ranked-grid')"
  key_links:
    - from: "src/store/store.ts fetchCollection/continueSession/completeSyncAll/reAuthAndResume/cancelSync"
      to: "view state"
      via: "set({ view: 'ranked-grid' })"
      pattern: "view: 'ranked-grid'"
---

<objective>
Change every AUTOMATIC `view: 'comparison'` landing/reset transition in `store.ts` to `view: 'ranked-grid'` (D-10) so grid becomes the app's default view everywhere the store lands or resets. Leave `backToComparison()` alone — it still sets `'comparison'` on demand, wired to the header's vs icon (D-11). Update the existing store tests that assert the old default and add explicit per-site coverage.

Purpose: Grid is the default view the user sees after login, resume, sync, and session reset.
Output: `store.ts` with five changed transition sites; `store.test.ts` with updated + added assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1.1-ui-cleanup/04.1.1-CONTEXT.md
@.planning/phases/04.1.1-ui-cleanup/04.1.1-PATTERNS.md
@src/store/store.ts
@src/store/store.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change the five automatic view transitions to ranked-grid in store.ts</name>
  <files>src/store/store.ts</files>
  <read_first>
    - src/store/store.ts (the six `view: 'comparison'` occurrences — lines 257, 293, 422, 539, 554, 569 — and the action definitions at 419-422)
    - .planning/phases/04.1.1-ui-cleanup/04.1.1-PATTERNS.md (store.ts section — the exact five sites to change and the one to leave)
  </read_first>
  <action>
    Edit `src/store/store.ts`, changing `view: 'comparison'` → `view: 'ranked-grid'` at exactly these five AUTOMATIC transition sites (D-10):
    - Line 257 — fetchCollection success (post-login/collection load).
    - Line 293 — continueSession (auto-resume of an existing session).
    - Line 539 — completeSyncAll's setTimeout auto-return after sync completes.
    - Line 554 — reAuthAndResume early-return when dirtyGameIds is empty.
    - Line 569 — cancelSync / session-expiry 401 reset.
    Do NOT change line 422 `backToComparison(): void { set({ view: 'comparison' }) }` — this is the only place `'comparison'` should remain as a target (D-11). Do NOT change the `view` union type on line 48 (`'ranked-grid'` is already a valid member). Make no other changes.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "view: 'comparison'" src/store/store.ts` returns exactly ONE line — the `backToComparison` action (formerly line 422).
    - `grep -c "view: 'ranked-grid'" src/store/store.ts` returns at least 5 (the five changed sites; note showRankedGrid also sets ranked-grid, so the count may be higher).
    - `backToComparison` still contains `set({ view: 'comparison' })`.
    - The `view` union type declaration is unchanged and still includes `'ranked-grid'`.
    - `npx tsc -b` passes.
  </acceptance_criteria>
  <done>The five automatic landing/reset transitions default to ranked-grid; backToComparison and the type union are unchanged; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update and extend store.test.ts default-view assertions</name>
  <files>src/store/store.test.ts</files>
  <read_first>
    - src/store/store.test.ts (existing view assertions at lines 84, 150, 1038, 1074 that assert 'comparison' after fetchCollection/login; the showRankedGrid/backToComparison block at lines 1008-1022; test helpers setupStoreWithGames/createMockStorage at lines 12-58)
    - src/store/store.ts (post-Task-1 transitions — the source of truth for expected view values)
  </read_first>
  <action>
    Edit `src/store/store.test.ts`:
    1. Update the four existing assertions that expect the old default to `toBe('ranked-grid')`: line 84 (`expect(state.view).toBe('comparison')` after fetchCollection), line 150 (after fetchCollection), line 1038 (after login auto-resume via continueSession), line 1074 (after login for a different user via fetchCollection). These now land on ranked-grid per D-10.
    2. Leave the showRankedGrid/backToComparison describe block (lines 1008-1022) unchanged — backToComparison still returns to 'comparison' (D-11), which is correct.
    3. ADD a new describe block "default landing view is ranked-grid (D-10)" with explicit tests: (a) after `fetchCollection('alice')` (mock a successful collection) `view` is 'ranked-grid'; (b) after `continueSession()` `view` is 'ranked-grid'; (c) after `cancelSync()` `view` is 'ranked-grid'. Use the existing createMockStorage/createAppStore/setupStoreWithGames helpers and the `../api/bggClient` mock already set up at the top of the file. Do NOT weaken any unrelated assertions.
  </action>
  <verify>
    <automated>npx vitest run src/store/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "toBe('comparison')" src/store/store.test.ts` corresponds only to backToComparison-related assertions (the four migrated lines now read `toBe('ranked-grid')`).
    - store.test.ts contains a describe block titled with "default landing view is ranked-grid (D-10)" asserting ranked-grid after fetchCollection, continueSession, and cancelSync.
    - `npx vitest run src/store/store.test.ts` exits 0.
    - Full `npx vitest run` suite exits 0.
  </acceptance_criteria>
  <done>Existing default-view assertions updated to ranked-grid, new explicit per-site tests added, backToComparison test preserved; store tests pass.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

No new exported symbols — value changes only:
- `src/store/store.ts`: five `view: 'comparison'` literals changed to `view: 'ranked-grid'` (fetchCollection, continueSession, completeSyncAll timer, reAuthAndResume, cancelSync).
- `src/store/store.test.ts`: new describe block "default landing view is ranked-grid (D-10)"; four existing assertions updated from 'comparison' to 'ranked-grid'.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store actions → view state | Purely internal SPA state; the change alters which client-only view is shown after existing transitions. No network, auth, or persistence boundary is affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.1-06 | Tampering | Default-view transitions | accept | Only the target `view` string changes; no change to auth, sessionId handling, dirty-flag guards, or persistence. cancelSync still nulls sessionId and 401 reset logic is untouched |
| T-04.1.1-07 | Information Disclosure | Post-401 reset lands on ranked-grid | accept | ranked-grid renders only already-loaded, non-secret collection data; no credential/session token is exposed by the view change |
| T-04.1.1-SC | Tampering | package installs | accept | No new dependencies installed in this phase (verified against package.json); package legitimacy gate not applicable |
</threat_model>

<verification>
- `npx vitest run src/store/store.test.ts` passes.
- Full `npx vitest run` suite passes.
- `npx tsc -b` passes.
- Exactly one `view: 'comparison'` remains in store.ts (backToComparison).
</verification>

<success_criteria>
- Five automatic transitions default to ranked-grid (D-10).
- backToComparison unchanged, still sets comparison (D-11).
- Store tests updated and green.
</success_criteria>

<output>
Create `.planning/phases/04.1.1-ui-cleanup/04.1.1-03-SUMMARY.md` when done.
</output>
