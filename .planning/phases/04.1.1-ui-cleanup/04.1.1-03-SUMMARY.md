---
phase: 04.1.1-ui-cleanup
plan: 03
subsystem: ui
tags: [zustand, state-management, react, vitest]

# Dependency graph
requires:
  - phase: 04.1.1-ui-cleanup (plan 01)
    provides: universal Header component with view-switch icon cluster (comparison/ranked-list/ranked-grid)
provides:
  - Grid view as the default landing/reset destination for all automatic store transitions
  - Explicit test coverage for the D-10 default-view contract (fetchCollection, continueSession, cancelSync)
affects: [04.1.1-ui-cleanup remaining plans, App.tsx view routing, RankedGridView]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "AUTOMATIC vs USER-INITIATED view transitions distinguished by intent — automatic landing/reset sites target 'ranked-grid' (D-10); only the explicit backToComparison() action (wired to the header's vs icon) targets 'comparison' (D-11)"

key-files:
  created: []
  modified:
    - src/store/store.ts
    - src/store/store.test.ts

key-decisions:
  - "Five automatic view-transition sites (fetchCollection success, continueSession, completeSyncAll timer, reAuthAndResume no-dirty early-return, cancelSync/401-reset) now set view: 'ranked-grid' instead of 'comparison', per D-10"
  - "backToComparison() and the view union type were left untouched — comparison remains a valid, deliberately-navigated-to state per D-11"

patterns-established:
  - "Default landing view is ranked-grid (D-10) — new tests added under this exact describe-block title so future plans can grep for the contract"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-07-17
---

# Phase 04.1.1 Plan 03: Default View Change to Ranked-Grid Summary

**Store's five automatic view transitions (login/collection-load, auto-resume, sync-complete, reauth-resume, session-reset) now land on ranked-grid instead of comparison; backToComparison() unchanged.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-17T15:26:00Z
- **Completed:** 2026-07-17T15:37:58Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- Changed exactly five `view: 'comparison'` literals to `view: 'ranked-grid'` in `store.ts`: `fetchCollection` success, `continueSession`, `completeSyncAll`'s auto-return timer, `reAuthAndResume`'s no-dirty-games early return, and `cancelSync`/401-reset.
- Left `backToComparison()` and the `view` union type declaration untouched, preserving the header's vs-icon navigation target (D-11).
- Updated four existing store test assertions from `toBe('comparison')` to `toBe('ranked-grid')` to match the new default.
- Added a new `describe('default landing view is ranked-grid (D-10)')` block with three explicit tests covering `fetchCollection`, `continueSession`, and `cancelSync`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Change the five automatic view transitions to ranked-grid in store.ts** - `84d445e` (feat)
2. **Task 2: Update and extend store.test.ts default-view assertions** - `c4ee2d4` (test)

**Plan metadata:** (pending — final docs commit below)

## Files Created/Modified
- `src/store/store.ts` - Five `view: 'comparison'` → `view: 'ranked-grid'` literal changes at the automatic landing/reset transition sites
- `src/store/store.test.ts` - Four assertions migrated to `ranked-grid`; new `describe('default landing view is ranked-grid (D-10)')` block added

## Decisions Made
- Automatic transitions (login/resume/sync-complete/reset) default to ranked-grid per D-10; only the explicit `backToComparison()` user action still targets comparison, per D-11.
- No changes to the `view` union type or to any USER-INITIATED navigation actions (`showRankedList`, `showUnplayedList`, `showRankedGrid`) — those were already correct.

## Deviations from Plan

None - plan executed exactly as written. Both tasks matched the plan's five specified sites and four specified test-assertion updates exactly; no additional sites were found during implementation.

## Issues Encountered
None. `npx tsc -b` reports 28 pre-existing type errors in `store.test.ts`/`store.ts` (unrelated `AppStore`/`RawGame` typing gaps, and one unused `redistribute` import) that predate this plan — verified via `git stash` comparison before and after both task commits, confirming an identical error count (28) in both states. These are out of scope for this plan per the note carried over from Plan 01's SUMMARY and are not touched by either task's `<files>` scope. `npx vitest run` shows 3 unhandled-rejection log lines from `src/api/bggClient.test.ts` (expected-error-path tests logging their rejections after assertion) — this file was not modified by this plan and the full suite still reports 197/197 tests passed, 7/7 files passed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Store-level default view is now ranked-grid everywhere the app automatically lands or resets; ready for any remaining 04.1.1 plans (e.g., Header/App.tsx wiring) that depend on this default.
- `npx vitest run src/store/store.test.ts` (69/69 passed) and full `npx vitest run` (197/197 passed) both green.
- Pre-existing `tsc -b` errors (28, unrelated to this plan's files) remain a known, documented gap — same count before and after this plan's changes.

---
*Phase: 04.1.1-ui-cleanup*
*Completed: 2026-07-17*

## Self-Check: PASSED

- FOUND: src/store/store.ts
- FOUND: src/store/store.test.ts
- FOUND: .planning/phases/04.1.1-ui-cleanup/04.1.1-03-SUMMARY.md
- FOUND: 84d445e (Task 1 commit)
- FOUND: c4ee2d4 (Task 2 commit)
