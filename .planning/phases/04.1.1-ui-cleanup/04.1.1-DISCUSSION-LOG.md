# Phase 04.1.1: ui-cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-17
**Phase:** 04.1.1-ui-cleanup
**Areas discussed:** Phase scope (freeform, since ROADMAP.md had no goal/SC), Header icons, Unplayed view header, Default view scope

---

## Phase scope

ROADMAP.md's Phase 04.1.1 entry was name-only ("ui cleanup", inserted urgent 2026-07-17) with no goal or success criteria — unusual, so scope had to be established from scratch rather than clarified.

| Option | Description | Selected |
|--------|-------------|----------|
| Visual polish | Spacing, alignment, colors, typography, responsive issues | |
| Navigation/menu behavior | Hamburger menu, view switching, button placement | ✓ (partial) |
| Comparison/pairing UX | How game pairs are presented/selected | |
| Something else — freeform | | ✓ |

**User's choice:** Freeform, detailed spec (verbatim):
> - update header and ensure it renders for all views: hamburger (sync, refresh, unplayed, logout), # this session · # total, vs (as icon), list (as icon), grid (as icon), [username]
> - back button should be removed from list and grid when header is universal
> - make grid the default view for the application
> - remove ranked list, grid view, and unplayed buttons from vs view
> - in vs view - move skip to right of games, make it red, make it vertically as high as the game selections, keep current width

**Notes:** This lands closest to "navigation/menu behavior" but is really its own category — a universal header replacing four separate per-view headers, plus a comparison-view layout cleanup. Note two prior ad-hoc UI fixes already shipped 2026-05-27 outside GSD phase tracking (hamburger menu 150ms close delay, rank badge removal from GameCard) — those are already in main and not part of this phase's remaining scope.

---

## Header icons

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse existing glyphs | ☰ for list, ⊞ for grid, new glyph for vs (e.g. ⚔ or "VS") | ✓ |
| Short text labels | "VS" / "List" / "Grid" as plain text | |
| User specifies exact glyphs | | |

**User's choice:** Reuse existing glyphs (recommended option).
**Notes:** Exact glyph for the new "vs" icon left to Claude's discretion (see CONTEXT.md D-03).

---

## Unplayed view header

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, same universal header | Unplayed gets hamburger/counts/icons/username like the other three views | ✓ |
| No, keep its current simple header | Unplayed keeps title + back-button only | |

**User's choice:** Yes, same universal header (recommended option).
**Notes:** Confirms D-04/D-05 in CONTEXT.md — Unplayed's back button is also removed.

---

## Default view scope

| Option | Description | Selected |
|--------|-------------|----------|
| Only initial landing | Just the post-login/collection-load transition becomes grid; post-sync and session-expiry keep landing on comparison | |
| All of them | Every automatic `view: 'comparison'` transition in store.ts becomes `view: 'ranked-grid'` | ✓ |

**User's choice:** All of them — deviated from the recommended narrower option.
**Notes:** Applies to all four `view: 'comparison'` set sites identified in store.ts (post-login/collection-load, post-sync, session-expiry/401 reset). `backToComparison()` itself is untouched — it's an explicit user action wired to the header's "vs" icon.

---

## Claude's Discretion

- Exact glyph/label for the new "vs" header icon
- Active-view highlight/disabled styling on header icons (not discussed)
- Exact flex/grid layout mechanics for placing Skip beside the two GameCards
- Hamburger open/close interaction details — reuse existing 150ms-delay pattern from ComparisonView

## Deferred Ideas

None — discussion stayed within the scope the user described. No scope creep occurred.
