---
phase: 05-production-deploy-render
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - render.yaml
  - proxy/functions/
  - firebase.json
  - .firebaserc
  - proxy/README.md
autonomous: true
requirements: [SC-1, SC-6]
must_haves:
  truths:
    - "A committed render.yaml Blueprint at repo root defines a free-tier Node web service rooted at proxy/server (SC-1)"
    - "All obsolete Firebase artifacts are deleted: proxy/functions/, firebase.json, .firebaserc (SC-6)"
    - "proxy/README.md documents the Render deployment + real req.path-based routing (no Firebase steps, no stale ?path= convention, no /api/geekrating)"
  artifacts:
    - path: "render.yaml"
      provides: "Render Blueprint: type web, runtime node, plan free, rootDir proxy/server"
      contains: "plan: free"
    - path: "proxy/README.md"
      provides: "Render deployment + verification documentation"
      contains: "render.yaml"
  key_links:
    - from: "render.yaml"
      to: "proxy/server/package.json"
      via: "rootDir + startCommand npm start"
      pattern: "rootDir: proxy/server"
---

<objective>
Create the committed `render.yaml` Blueprint (D-04) that defines the free-tier Node web service, delete the obsolete Firebase artifacts that were never actually deployed (D-02), and rewrite `proxy/README.md` for Render (D-11). This turns the proxy code from plan 05-01 into a git-native, deployable, correctly-documented service.

Purpose: Make the proxy deployable via Render's Blueprint sync and remove the stale/misleading Firebase footprint and docs.
Output: `render.yaml` (repo root), deletion of `proxy/functions/` + `firebase.json` + `.firebaserc`, rewritten `proxy/README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-production-deploy-render/05-CONTEXT.md
@.planning/phases/05-production-deploy-render/05-RESEARCH.md
@.planning/phases/05-production-deploy-render/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the render.yaml Blueprint at repo root</name>
  <files>render.yaml</files>
  <read_first>
    - .planning/phases/05-production-deploy-render/05-RESEARCH.md ("Architecture Patterns" Pattern 2 + "Code Examples" — the CITED Blueprint template; "Common Pitfalls" Pitfalls 3-4)
    - .planning/phases/05-production-deploy-render/05-PATTERNS.md ("render.yaml" section)
    - firebase.json — the role-analog descriptor being replaced (its `source: proxy/functions` maps to render.yaml's `rootDir: proxy/server`)
  </read_first>
  <action>
    Create `render.yaml` at repo root (Claude's Discretion default per D-Discretion; repo-root is the conventional Blueprint location and does not conflict with existing root config). Define a single service:
    - `type: web`
    - `name: bgg-ranker-proxy`
    - `runtime: node`
    - `plan: free` — MUST be set explicitly; the documented default is `starter` (paid) per Pitfall 3.
    - `region: oregon`
    - `rootDir: proxy/server` — build/start commands run from here and auto-deploy triggers scope to this dir. MUST match where plan 05-01 created the package.json.
    - `buildCommand: npm install`
    - `startCommand: npm start` — MUST match `proxy/server/package.json`'s `"start": "node server.js"` from plan 05-01.
    - `healthCheckPath: /healthz` — matches the route added in plan 05-01's server.js.
    - `envVars`:
      - `NODE_VERSION` = `20.18.0` (pin explicitly; Render may not honor `engines.node` alone per Pitfall 4 — belt-and-suspenders).
      - `ALLOWED_ORIGIN` = `"*"` with `sync: false` (dashboard-editable later without a code change; wildcard is an accepted risk for this phase per T-05-03 / Assumption A3, since the SPA→proxy leg uses a custom header, not credentialed cookies).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('render.yaml','utf8'); for (const k of ['type: web','runtime: node','plan: free','rootDir: proxy/server','startCommand: npm start','healthCheckPath: /healthz','NODE_VERSION','ALLOWED_ORIGIN']) if(!y.includes(k)){console.error('MISSING: '+k);process.exit(1)} console.log('render.yaml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `render.yaml` exists at repo root and contains all of: `type: web`, `runtime: node`, `plan: free`, `rootDir: proxy/server`, `buildCommand: npm install`, `startCommand: npm start`, `healthCheckPath: /healthz`
    - `plan: free` is present verbatim (guards against the paid-default trap)
    - `NODE_VERSION` is pinned to `20.18.0` and `ALLOWED_ORIGIN` carries `sync: false`
    - `startCommand` value equals what `proxy/server/package.json` `start` script resolves to (`node server.js`)
  </acceptance_criteria>
  <done>A valid free-tier Node Blueprint exists at repo root, wired to proxy/server with a matching health check path.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the obsolete Firebase artifacts</name>
  <files>proxy/functions/, firebase.json, .firebaserc</files>
  <read_first>
    - .planning/phases/05-production-deploy-render/05-CONTEXT.md (D-02 — exact deletion list; canonical_refs confirming .firebaserc was never configured)
    - .planning/phases/05-production-deploy-render/05-RESEARCH.md ("Runtime State Inventory" — confirms the Firebase Function was never deployed; nothing live to decommission, deletion is safe and additive-free)
    - firebase.json, .firebaserc — confirm content before removal (firebase.json references proxy/functions; .firebaserc still has the YOUR_FIREBASE_PROJECT_ID placeholder)
  </read_first>
  <action>
    Delete the obsolete Firebase footprint per D-02 using `git rm`:
    - `git rm -r proxy/functions` (removes `src/index.ts`, `package.json`, `tsconfig.json`, `package-lock.json`, and the tracked subtree).
    - `git rm firebase.json`
    - `git rm .firebaserc`
    Nothing needs migrating — the Render service in plan 05-01 is a fresh implementation, not a port, and the Firebase Function was never live (placeholder project ID). The gitignored `proxy/functions/lib/` compile output disappears structurally with the source dir. Do not touch `proxy/README.md` in this task (Task 3 rewrites it) and do not touch any `.env*` file.
  </action>
  <verify>
    <automated>test ! -e proxy/functions && test ! -e firebase.json && test ! -e .firebaserc && echo "firebase artifacts removed"</automated>
  </verify>
  <acceptance_criteria>
    - `proxy/functions/` no longer exists (directory and all contents removed)
    - `firebase.json` no longer exists
    - `.firebaserc` no longer exists
    - `git status` shows these as staged deletions (removed from tracking, not just the working tree)
    - `proxy/README.md` still exists (handled by Task 3, not deleted here)
  </acceptance_criteria>
  <done>All three Firebase artifacts are deleted and staged for removal from git (SC-6).</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite proxy/README.md for Render</name>
  <files>proxy/README.md</files>
  <read_first>
    - proxy/README.md — current Firebase-oriented content being fully rewritten (preserve useful structural sections, replace all Firebase/stale specifics)
    - .planning/phases/05-production-deploy-render/05-PATTERNS.md ("proxy/README.md (docs, full rewrite)" — exact list of what to keep/replace/fix)
    - .planning/phases/05-production-deploy-render/05-CONTEXT.md (D-11) and 05-RESEARCH.md (Pitfall 5 — the stale ?path= convention; "Code Examples" — real client contract)
    - src/api/bggClient.ts (lines 117-184) — the real endpoints: PUT /api/collectionitem/{collId}, POST /login/api/v1, GET /xmlapi2/collection
  </read_first>
  <action>
    Full rewrite of `proxy/README.md` (D-11 — a rewrite, not a patch). Title it for the Render proxy (e.g. "BGG Proxy — Render Web Service"). Include these sections:
    - **Overview** — Node + Express reverse proxy that adds CORS headers and relays BGG's 3-cookie session; lives in `proxy/server/`, deployed to Render via the repo-root `render.yaml` Blueprint.
    - **Production Deployment (Render)** — replace ALL Firebase steps (`firebase login`, `.firebaserc` project ID, Blaze plan requirement, `firebase deploy`) with the Render flow: (1) commit `render.yaml` + `proxy/server/`, (2) in the Render dashboard connect the GitHub repo and create/sync the Blueprint (browser-only OAuth step — no CLI equivalent), (3) copy the live service URL (e.g. `https://bgg-ranker-proxy.onrender.com`) into `.env.production` as `VITE_BGG_API_BASE`, (4) rebuild the SPA. Note the free-tier ~30-60s cold start after 15 min idle.
    - **Proxy Interface** — document the REAL routing shape: the proxy mirrors BGG's path structure directly (NO `?path=` wrapper). Examples: `GET <RENDER_URL>/xmlapi2/collection?username=X&own=1&subtype=boardgame`, `POST <RENDER_URL>/login/api/v1`, `PUT <RENDER_URL>/api/collectionitem/{collId}` with an `X-BGG-Session` header. Do NOT reference `/api/geekrating` (stale — the real write endpoint is `/api/collectionitem/{collId}`).
    - **Session Token Handling** — keep the substantively-correct explanation: proxy captures BGG's full Set-Cookie set server-side, returns only `{sessionId}` JSON, never relays Set-Cookie to the SPA (AUTH-03 / D-08); note the 3-cookie relay (SessionID + bggusername + bggpassword) is what the server-side cache holds.
    - **Quick Verification** — `curl -f <RENDER_URL>/healthz` (expect 200), a collection-read curl example against the real path, and the smoke test invocation: `RENDER_URL=... BGG_USERNAME=... BGG_PASSWORD=... node scripts/smoke-test-render.mjs` (NOT `bash scripts/smoke-test-prod.sh` — that never existed).
    Remove every mention of Firebase, Blaze, `firebase deploy`, `.firebaserc`, `us-central1-...cloudfunctions.net`, the `?path=` convention, and `/api/geekrating`.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('proxy/README.md','utf8').toLowerCase(); const bad=['firebase','blaze','?path=','geekrating','cloudfunctions.net','.firebaserc']; for(const b of bad) if(t.includes(b.toLowerCase())){console.error('STALE REF: '+b);process.exit(1)} for(const g of ['render.yaml','/healthz','/api/collectionitem/','smoke-test-render.mjs']) if(!t.includes(g.toLowerCase())){console.error('MISSING: '+g);process.exit(1)} console.log('README OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `proxy/README.md` contains none of: `firebase`, `blaze`, `?path=`, `geekrating`, `cloudfunctions.net`, `.firebaserc` (case-insensitive)
    - `proxy/README.md` contains: `render.yaml`, `/healthz`, `/api/collectionitem/`, `smoke-test-render.mjs`
    - The Proxy Interface section documents direct `req.path`-style routing (BGG paths mirrored), not a `?path=` query wrapper
    - The Quick Verification section references `node scripts/smoke-test-render.mjs`, not any bash smoke script
  </acceptance_criteria>
  <done>proxy/README.md accurately documents the Render deployment and real proxy routing, with all Firebase/stale references removed (D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Any web origin → Render proxy | With wildcard CORS, any page can route calls through the proxy (still needs real BGG creds to authenticate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Spoofing (of origin) | `render.yaml` `ALLOWED_ORIGIN: "*"` | accept | Accepted risk for this phase (Assumption A3): SPA→proxy leg uses a custom `X-BGG-Session` header (not credentialed cookies), so open-relay abuse still cannot obtain a BGG session without real credentials. `sync: false` makes it a dashboard-only tightening once a permanent SPA origin exists — no code change/redeploy needed |
</threat_model>

<verification>
- `render.yaml` present with `plan: free`, `rootDir: proxy/server`, matching `startCommand`/`healthCheckPath` (SC-1 Blueprint deliverable)
- `test ! -e proxy/functions && test ! -e firebase.json && test ! -e .firebaserc` succeeds (SC-6)
- `proxy/README.md` grep gate: no Firebase/stale refs, contains Render specifics (D-11)
</verification>

<success_criteria>
- Committed Blueprint defines the free-tier Node service (SC-1)
- All obsolete Firebase artifacts removed (SC-6)
- Documentation accurately reflects the Render deployment and real routing (D-11)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 05-02)

**New files:**
- `render.yaml` (repo root) — Render Blueprint; service `bgg-ranker-proxy`; keys: `type: web`, `runtime: node`, `plan: free`, `region: oregon`, `rootDir: proxy/server`, `buildCommand: npm install`, `startCommand: npm start`, `healthCheckPath: /healthz`; envVars `NODE_VERSION=20.18.0`, `ALLOWED_ORIGIN="*"` (`sync: false`)

**Deleted files:**
- `proxy/functions/` (src/index.ts, package.json, tsconfig.json, package-lock.json)
- `firebase.json`
- `.firebaserc`

**Rewritten files:**
- `proxy/README.md` — Render deployment + verification documentation

**Follow-up doc fixes flagged (NOT edited this phase):** `CLAUDE.md` "Key constraint" line still names "a Cloudflare Worker" (stale — now Render); `PROJECT.md` Key Decisions row still names Firebase Cloud Functions. Flag both for a doc-accuracy fix at phase close (D-01, canonical_refs).
</artifacts_produced>

<output>
Create `.planning/phases/05-production-deploy-render/05-02-SUMMARY.md` when done
</output>
