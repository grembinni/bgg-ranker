---
phase: 05-production-deploy-render
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - .env.production
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5]
user_setup:
  - service: render
    why: "Deploy the CORS proxy; the initial GitHub-repo connection is a browser-only OAuth flow (D-05)"
    dashboard_config:
      - task: "Connect the GitHub repo and create/sync the render.yaml Blueprint, then copy the live service URL"
        location: "Render Dashboard → New → Blueprint → connect GitHub repo"
  - service: bgg
    why: "Real credentials are required to run the authenticated smoke test (login + rating write)"
    env_vars:
      - name: BGG_USERNAME
        source: "The user's BGG account username"
      - name: BGG_PASSWORD
        source: "The user's BGG account password (passed as a process env var at smoke-test time; never written to disk)"
must_haves:
  truths:
    - "The Render web service is live and GET <RENDER_URL>/healthz returns HTTP 200 (SC-1)"
    - ".env.production has VITE_BGG_API_BASE set to the live Render service URL (SC-3)"
    - "node scripts/smoke-test-render.mjs exits 0 against the live URL — collection read, login, and rating write all succeed (SC-2, SC-4)"
    - "npm run build succeeds and the built SPA reaches BGG through the Render proxy with no CORS errors (SC-5)"
  artifacts:
    - path: ".env.production"
      provides: "Production API base pointing at the live Render proxy"
      contains: "VITE_BGG_API_BASE=https://"
  key_links:
    - from: ".env.production VITE_BGG_API_BASE"
      to: "live Render service URL"
      via: "build-time static replacement baked into the SPA bundle"
      pattern: "VITE_BGG_API_BASE=https://"
---

<objective>
Take the committed proxy code (05-01) and Blueprint (05-02) live on Render, wire the SPA to the live URL, and prove the full production path end-to-end. This plan is NON-AUTONOMOUS: the GitHub-repo-to-Render connection is a browser-only OAuth flow (D-05, 05-RESEARCH.md Environment Availability) that an executor agent cannot perform — a human must connect the repo and trigger the Blueprint sync, then the executor resumes to verify.

Purpose: Achieve the phase goal — the app runs end-to-end in production with no CORS errors, with the authenticated write path proven against live BGG.
Output: A live Render service URL, updated `.env.production`, a green smoke test, and a successful production build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-production-deploy-render/05-CONTEXT.md
@.planning/phases/05-production-deploy-render/05-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human connects the GitHub repo to Render and syncs the Blueprint</name>
  <what-built>
    Plans 05-01 and 05-02 committed the full deployable proxy: `proxy/server/` (Express app + package.json), the repo-root `render.yaml` Blueprint (free-tier Node service, health check `/healthz`), and the smoke test. All code is in git and ready for Render to build. Before this checkpoint, the executor MUST confirm those commits are pushed to the GitHub remote (`git status` clean, `git push` complete) so Render can see them.
  </what-built>
  <how-to-verify>
    1. Open the Render dashboard (you already have an account — D-05) at https://dashboard.render.com
    2. Click New → Blueprint, and connect this GitHub repository (browser OAuth — grant Render access). This is the one step no CLI can do.
    3. Render reads the committed `render.yaml`; confirm it shows a service named `bgg-ranker-proxy`, plan `free`, rootDir `proxy/server`. Apply / sync the Blueprint.
    4. Wait for the first build+deploy to go live (first build may take a few minutes; free tier has a ~30-60s cold start).
    5. Copy the live service URL (it looks like `https://bgg-ranker-proxy.onrender.com`).
    6. In a browser or terminal, hit `<RENDER_URL>/healthz` and confirm it returns HTTP 200.
    7. Have your BGG username and password ready — the next task's smoke test needs them as env vars.
  </how-to-verify>
  <resume-signal>Paste the live Render service URL (and confirm /healthz returned 200). Type "approved &lt;URL&gt;" or describe any deploy errors.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Point .env.production at the live URL and run the live smoke test</name>
  <files>.env.production</files>
  <read_first>
    - .env.production — current value is an empty `VITE_BGG_API_BASE=` (confirmed via git); replace with the live Render URL
    - .env.development — sibling file, same key, value `/bggapi` (do NOT modify; reference only for the key format)
    - scripts/smoke-test-render.mjs — the smoke test authored in plan 05-01, run here against the live URL
    - .planning/phases/05-production-deploy-render/05-RESEARCH.md ("Common Pitfalls" Pitfall 2 — cold-start retry expectation)
  </read_first>
  <action>
    Using the live Render URL provided by the Task 1 checkpoint (call it `<RENDER_URL>`):
    1. Edit `.env.production` so the single line reads `VITE_BGG_API_BASE=<RENDER_URL>` (the live `https://...onrender.com` URL — no trailing slash, no `/bggapi`). This is the only change to that file (SC-3).
    2. Run the live smoke test with real credentials supplied by the user as process env vars (never written to disk): `RENDER_URL=<RENDER_URL> BGG_USERNAME=<user> BGG_PASSWORD=<pass> node scripts/smoke-test-render.mjs`. If the user has not yet supplied credentials, pause and request them before running. Expect all three checks (collection read, login → {sessionId}, rating write) to PASS and the process to exit 0. If the first attempt fails on a cold start, the script's `withColdStartRetry` should recover; if it exits 1 for a real reason, capture the failing step and report it rather than editing the proxy here (proxy fixes belong in a 05-01 revision / gap-closure).
  </action>
  <verify>
    <automated>grep -E '^VITE_BGG_API_BASE=https://' .env.production && echo "env OK (run: RENDER_URL=<url> BGG_USERNAME=<u> BGG_PASSWORD=<p> node scripts/smoke-test-render.mjs → expect exit 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.production` contains exactly one `VITE_BGG_API_BASE=` line whose value is the live `https://...` Render URL (SC-3)
    - `node scripts/smoke-test-render.mjs` against the live URL exits 0 with all three steps PASS (SC-2, SC-4) — collection read, login returning a non-empty sessionId, and a 2xx rating write
    - The rating write step returning 2xx proves the 3-cookie relay works end-to-end against live BGG (the specific gap D-07 exists to fix)
  </acceptance_criteria>
  <done>.env.production points at the live proxy and the real-credential smoke test exits 0 (SC-2, SC-3, SC-4).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the production build reaches BGG through the proxy with no CORS errors</name>
  <what-built>
    `.env.production` now points `VITE_BGG_API_BASE` at the live Render proxy. The executor has run `npm run build` (which bakes that URL into the bundle) followed by `npm run preview`. This checkpoint confirms the built SPA actually talks to BGG through the Render proxy with no CORS errors (SC-5) — the one check that requires visual DevTools inspection.
  </what-built>
  <how-to-verify>
    1. Confirm the executor's `npm run build` completed with no errors (SC-5 build half).
    2. With `npm run preview` running, open the previewed app (e.g. http://localhost:4173) in a browser.
    3. Open DevTools → Network tab.
    4. Log in with your BGG credentials and load your collection so the SPA makes real requests through the proxy.
    5. Confirm the requests go to the live Render URL and there are NO CORS errors in the Console/Network tabs (no "blocked by CORS policy" messages); collection loads and a rating write succeeds.
  </how-to-verify>
  <resume-signal>Type "approved" if the collection loads and there are no CORS errors, or describe the errors seen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built SPA → live Render proxy → BGG | The full production path goes live in this plan; same boundaries as 05-01, now exercised with real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Spoofing (of origin) | Live wildcard-CORS proxy | accept | Carried from 05-02: `ALLOWED_ORIGIN: "*"` accepted for this phase (custom-header SPA↔proxy leg, no credentialed cookies); tighten via the `sync: false` dashboard env var once a permanent SPA hosting origin exists. Document the live wildcard state in the SUMMARY |
| T-05-04 | Information Disclosure | Live /login response | mitigate | Confirmed live by the smoke test: login returns `{sessionId}` JSON only; the browser DevTools check (Task 3) confirms no Set-Cookie with bggusername/bggpassword reaches client JS |
</threat_model>

<verification>
- `<RENDER_URL>/healthz` returns 200 (SC-1, confirmed at Task 1 checkpoint)
- `.env.production` `VITE_BGG_API_BASE` is the live https Render URL (SC-3)
- `node scripts/smoke-test-render.mjs` exits 0 against live URL (SC-2, SC-4)
- `npm run build` succeeds and DevTools shows no CORS errors through the proxy (SC-5)
</verification>

<success_criteria>
- Render service live via the committed Blueprint (SC-1)
- 3-cookie relay proven on the live authenticated write path (SC-2)
- .env.production points at the live URL (SC-3)
- Smoke test exits 0 (SC-4)
- Production build succeeds with no CORS errors (SC-5)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 05-03)

**Modified files:**
- `.env.production` — `VITE_BGG_API_BASE` set to the live Render service URL (e.g. `https://bgg-ranker-proxy.onrender.com`)

**Runtime outputs (not files):**
- A live Render web service URL (`bgg-ranker-proxy`) — recorded in the plan SUMMARY
- Green live smoke-test run (collection read + login + rating write)
- Production build verified CORS-clean in-browser

**Follow-up flagged (NOT this phase):** once a permanent public SPA hosting origin exists, tighten `ALLOWED_ORIGIN` from `"*"` to that origin via Render's dashboard (`sync: false` env var — no redeploy needed).
</artifacts_produced>

<output>
Create `.planning/phases/05-production-deploy-render/05-03-SUMMARY.md` when done
</output>
